# SDD ledger — plan: /c/unirefund/docs/superpowers/plans/2026-09-04-app-config-provider-4-pos-app.md

Repo: C:/unirefund/pos-app, ISOLATED WORKTREE at C:/unirefund/.worktrees/posapp-app-config (OUTSIDE the repo — neither .worktrees nor .claude/worktrees is gitignored in pos-app, so an inside-the-repo worktree would be committable), branch feat/app-config-provider off main @ 792f4e2.
Spec + plan live in the separate docs repo at C:/unirefund/docs.

## Environment (measured 2026-09-08, not quoted)
- pos-app main checkout is CLEAN and has **no other sessions and no nested worktrees** — materially safer than super-app, where a locked sibling worktree is live.
- `.env` present with EXPO_PUBLIC_GATEWAY_URL, SUPPORTED_LOCALES, EXPO_PUBLIC_GLITCHTIP_DSN. Copied to the worktree; `npm install` done, node_modules is a real dir.
| Gate | Baseline |
| --- | --- |
| `npm run typecheck` | clean, 0 errors |
| `npm run lint` | **0 errors, 35 warnings** (matches the plan's recorded figure) |
| `npm test` | **31 suites / 375 tests — ALL PASSING, zero failures** |
**Plan said 30 suites / 361 tests — drifted to 31/375.** Re-measured, as the plan instructs.
**pos-app has ZERO pre-existing test failures.** That is a much better signal than plan 3 had: any failure here is unambiguously a regression, with no flake-vs-deterministic judgement to make.
Plan says pos-app is 27 commits behind core/main; it is now **43** (was 40 before core-mobile#23 landed). That does not change the plan's approach — it strengthens its reason for mirroring core rather than merging it.

## Dependency: SATISFIED, and this is why the port came first
core-mobile#23 (`4608792`) is merged. Verified on core/main: NormalizedApplicationConfiguration present, clearGrants x3, clearGrantedPolicies x2. **pos-app will therefore mirror a CORRECTED core** rather than copying two fail-open bugs into a third app and needing a follow-up to undo them.

## Plan amendment
The plan predates the port, so its transcribed code blocks are stale. Amended (docs 46de423): **core's actual source outranks this plan's code blocks.** Six ported items enumerated there, including that `revokeSessionAuthority()` belongs on the validation early-return the plan already describes at ~line 324, and that the `Partial<Record<Policies, boolean>>` widening will surface strict-null errors in pos-app that must be fixed at the type level, not cast away.

## Pre-flight conflict scan
| Rows checked | Finding |
| --- | --- |
| T1 (pure layer) -> T2 (store) | T2 imports T1's types/parsers. Strictly sequential; T1 creates files that compile standalone. No conflict. |
| T2 (store) -> T4 (wiring) | T4 consumes the store's actions. Sequential. |
| T3 (action) -> T4 (wiring) | T4 calls `getApplicationConfigurationApi`. Sequential. |
| T1 & T3 both touch `src/actions/AccountService/` | T1 creates only `src/config/*`; T3 owns the action file. No overlap. |
| T4 internal | Single task touching SessionProvider + user store + `_layout` + consumers. Coupled by design — splitting them would leave a non-compiling intermediate, which is exactly the defect plan 3 hit twice. Correctly grouped. |
| T5 | Verification only, modifies nothing. |
| Non-compiling intermediates? | **None.** T1 pure files compile alone; T2 compiles against T1; T3 is a self-contained action change; T4 integrates. This plan is better decomposed than plan 3 was. |
| Plan vs review rubric | No no-op tests, no mandated duplication. Clean. |
Scan clean — no rulings needed before Task 1.

## Standing constraints
- **Mirror core's ACTUAL source** (core/main @ 4608792), not this plan's stale transcriptions. Keep file paths and symbol names byte-identical to core so a future `git merge core/main` resolves mechanically — that is the plan's whole reason for mirroring rather than merging.
- pos-app's `fetchRequest` returns `ApiResult<T>` = `{success:true,data,status} | {success:false,data:null,message,status}` and **never throws**. Do not wrap it in try/catch expecting rejections. This is the one place the port cannot be copied verbatim.
- Time zone MUST come from `timing.timeZone.iana.timeZoneName`, never `setting.values["Abp.Timing.TimeZone"]` (a Windows id).
- Boolean setting parsing MUST be case-insensitive — payloads carry both "True" and "false".
- `policies` and `user` MUST NOT be persisted — only `country` and `settings`.
- pos-app ships GlitchTip: **do not log configuration payloads**, `currentUser` carries an email and a session id.
- `rn-mlkit-ocr` needs the global jest mock; it ships untranspiled ESM and registers a TurboModule at import, so any suite transitively reaching it fails to PARSE.
- prettier is NOT a gate (228 files fail at baseline). Files are LF here, so `--write` on your own files will not churn line endings.

## Task 1
Status DONE. Commit f3db2649. Gates: typecheck clean; lint 0 errors / **35 warnings = baseline**; `npm test` **34 suites / 405 tests all passing** (baseline 31/375 + 3 suites / 30 tests). Zero failures anywhere.
9 files created under src/config/, **all byte-identical to core's post-fix source at 4608792** — verified with `diff` against `git show origin/main:...` per file, zero output on every one. Read from the POST-PORT commit, so the two fail-open fixes are inherited rather than reproduced.
**No adaptation needed at all**: the two SDK type names resolve unchanged in pos-app's generated types.gen.ts, and pos-app's AccountService/AdministrationService barrels are themselves byte-identical to core's, with existing pos-app code already importing through them — so my brief's caution about barrel differences did not apply.
**Good self-correction worth recording:** the agent ran `prettier --write`, saw it reformat two files (both repos share printWidth 80 but core's lines exceed it), and **reverted to core's exact bytes before committing** — reasoning that byte-identical mirroring is the whole point of the task. That is precisely the lesson plan 3 learned the hard way as its Important 3, arrived at independently here.
Confirmed: normalizeApplicationConfiguration returns NormalizedApplicationConfiguration and emits `country: null` on absence (pinned by the test "reports no country at all when country-settings is unavailable"); time zone from `timing.timeZone.iana.timeZoneName`; case-insensitive boolean parsing; `||` currency coercion retained. No casts on production lines; the fixture's `as unknown as RawApplicationConfiguration` mirrors core and an existing pos-app test convention.
Task 1: review **APPROVED** — spec ✅, 0 Critical, 0 Important, 1 trivial Minor (the report said the `as unknown as` cast precedent exists in "5 other test files"; there are 5 other files but only 3 are tests — the convention holds, the count was loose).
**Reviewer independently confirmed 4608792 IS the POST-FIX commit** — the check that actually mattered, since mirroring the pre-fix commit would have looked perfect while silently reintroducing both fail-open bugs into a third app. It verified the merge is PR #23, that `git diff 4608792 eafdc34 -- src/config/` is EMPTY, and that `git merge-base --is-ancestor 4608792 origin/main` is true.
Fidelity verified TWO independent ways per file (diff after CRLF-stripping, plus hash-object cross-checked against byte counts and the diff's own blob SHAs): all 9 files byte-identical, and both sides turned out to be LF already so no stripping was even needed. Scope confirmed by `--name-status`: exactly 9 files, all additions, nothing under src/actions|store|providers, no lockfile change, and the two pre-existing src/config files untouched.
Invariants confirmed by reading the mirrored source rather than trusting the report: `country: countryInfo ? {...} : null` (a real null, pinned by a test asserting toBeNull); time zone from `timing.timeZone.iana.timeZoneName` with `appConfigKeys.test.ts` POSITIVELY asserting the Windows key is absent from SETTING_KEYS; `getBooleanSetting` trims+lowercases with three cases; `||` currency coercion with a dedicated empty-string test. 30 `it()` blocks counted, matching the report exactly.
Task 1: complete (commit f3db2649, approved, no fix round)

## Task 2
Status DONE. Commit e0013ff1. Gates: typecheck clean; lint 0 errors / **35 warnings = baseline**; **35 suites / 416 tests all passing** (34/405 + 11 new). Zero failures.
Store + tests copied verbatim from core's post-fix source (4608792). **Byte-identity checked THREE times**: against `git show origin/main:...` before appending anything, again after, and once more on the COMMITTED blob via `git show HEAD:...` restricted to the mirrored region — zero output every time. Test file diffs to zero over its full length. Both files LF, matching core; no prettier run.
**Divergence made STRUCTURALLY visible rather than just disclosed:** a `// Everything below is a pos-app-only addition` marker at line 171. Above it is core's file untouched; below it are `useCountryCode2`/`useCountryName` (pos-app's print template needs countryName, which core has no consumer for) and `DEFAULT_CURRENCY_CODE`/`getCurrencySymbol`/`CURRENCY_SYMBOLS` ported from the local country-settings store, all of which plan 4's own "Produces" list requires from this module. **No divergence inside the mirrored portion.** This also matches what super-app did — the same helpers live in the same file there — so the three apps stay consistent, which is the pattern the user asked for.
Confirmations: `setConfiguration` takes NormalizedApplicationConfiguration and resolves `country ?? state.configuration.country` (two passing tests pin it); `clearGrants()` clears policies/settings/features and keeps country; **`partialize` persists ONLY country and settings, with settings further filtered to SETTING_KEYS** — never policies, never user — confirmed by source and two dedicated tests. Load-bearing docblocks carried verbatim. No new casts (the two that exist are inherited byte-for-byte from core inside the mirrored region). No config-payload/PII logging, which matters here because pos-app ships GlitchTip.
Task 2: review **APPROVED** — spec ✅, 0 Critical, 1 Important (a PLAN defect, flagged for Task 4), 3 Minor.
Fidelity independently verified: reviewer wrote core's blobs to scratch and diffed byte-for-byte, confirmed `origin/main` resolves to 4608792 (the #23 merge), and confirmed both sides are LF via `grep -c $'\r'`. Mirrored region byte-identical; test file byte-identical in full. Post-fix behaviour confirmed from source: setConfiguration takes NormalizedApplicationConfiguration and resolves `country ?? state...country`; clearGrants keeps country; **partialize returns only `{configuration:{country,settings}}` — no policies, no user, ever**, pinned by two tests that invoke `persist.getOptions().partialize()` directly and assert the key set is exactly ["country","settings"], plus a third pinning merge's stale-blob guard by object identity.
`getCurrencySymbol`/`DEFAULT_CURRENCY_CODE`/`CURRENCY_SYMBOLS` verified byte-for-byte against pos-app's own country-settings.ts — **the equivalence claim holds exactly, fallback semantics included.**
**IMPORTANT — MY PLAN DEFECT #10, and it would have bitten Task 4 mid-wiring.** My Step 2 snippet mandated `useCountryCode2`/`useCountryName` as React hooks. Their stated and ONLY real consumer is `tagPrintTemplateV3` (src/screens/(auth)/Tags/TagDetail/_components/tagPrintTemplate.ts:326) — a plain `async function` invoked imperatively from an onPress handler, which today reads `useCountrySettingsStore.getState().countrySettings?.countryName`, a NON-hook accessor. This repo's eslint spreads `pluginReactHooks.configs.recommended.rules`, so calling a hook there is a `react-hooks/rules-of-hooks` ERROR, not a warning. **There is no existing hook these two "replace" — what they replace is an imperative accessor.** Reviewer correctly ruled it a plan-level issue rather than a Task 2 execution failure, since the shape was mandated verbatim by the brief.
MINOR — **"Move" vs "duplicate"**: my prose said MOVE the currency helpers, the implementer DUPLICATED them, and country-settings.ts still carries its own untouched copies. Reviewer judges the duplication almost certainly correct (Task 2's Files list never included country-settings.ts, and deleting now would break existing consumers, which is Task 4's scope) — but it leaves **two independently-driftable copies of CURRENCY_SYMBOLS** that Task 4 must reconcile, and the report should have flagged it the way it flagged the inherited casts.
MINOR: marker is at line 172, not 171 (171 is blank). MINOR: no tests for getCurrencySymbol/useCountryCode2/useCountryName — consistent with country-settings.ts never having had any, so not a regression, but worth closing before 7+ consumers lean on them.
Merge-hazard question answered: appending after a clear marker is what the brief instructed and is the lowest-friction option; a future `git merge core/main` conflicts only if core appends at EOF too, which is narrow and self-evident rather than silently swallowed. Acceptable tradeoff, matches super-app.
Task 2: complete (commit e0013ff1, approved, no fix round — the Important and the duplicate-helpers Minor both fold into Task 4)
**CORRECTION to the Task 2 review's Important, verified by me against pos-app source.** The reviewer's finding assumed Task 4 would call `useCountryName()` inside `tagPrintTemplateV3`. **It will not** — plan 4's Task 4 Step 4 already prescribes `useApplicationConfigurationStore.getState().configuration.country.countryName ?? ""` and states in prose "This is a module-scope read outside React, which is why it uses getState() — keep it that way." So there is no rules-of-hooks error waiting in Task 4, and no plan patch is needed for that.
**But it surfaced a REAL adjacent problem: `useCountryCode2` is a DEAD EXPORT in pos-app.** Grepped: the only `countryCode2` references outside src/saas are `CountryInput.tsx`/`CountrySelectionModal.tsx`, which use their OWN local type built from `country.alpha2` and have nothing to do with the configuration store. `countryName` likewise has exactly one reader — the print template's imperative `.getState()` call — so `useCountryName` as a HOOK is also unconsumed. **My Task 2 brief justified both selectors with "pos-app's print template needs countryName", which was wrong: the print template does not use a hook.** Plan defect #11, and the same class as #10 — I asserted a consumer without checking how it reads.
Measured for Task 4: `useCurrencyCode`/`useCurrencySymbol` DO have 5 real consumers (LocalizationProvider, AddProductScreen, SaleScreen, SaleScreenV2, CartReviewModal) plus the store itself, so the currency half of the additions is genuinely load-bearing.
Ruling: Task 4 reconciles this **after** migrating consumers — keep whichever selectors end up consumed, delete the ones that do not. This project has already removed dead surface for exactly this reason (plan 3 dropped `formatToTimezoneDate`), so leaving an unconsumed export would be inconsistent. Patching Task 4 rather than spinning a Task 2 fix round, since the export is inert until something imports it.

## Task 3
Status DONE. Commit f25f10a2. Gates: **typecheck 2 errors, both in SessionProvider.tsx** (Task 4's file, from the two deleted exports); lint 0 errors / 35 warnings = baseline; **36 suites / 422 tests all passing** (35/416 + 1 suite / 6 tests).
**MY PRE-FLIGHT SCAN WAS WRONG — defect #12, and it is the same class I hit twice in plan 3.** I wrote "No non-compiling intermediates. ... T3 is a self-contained action change." It is not: Task 3 deletes `getGrantedPoliciesApi`/`getCurrentUserIdApi`, which SessionProvider still imports, so the tree does not compile until Task 4 rewires it. I even congratulated this plan for being better decomposed than plan 3 while missing the identical problem.
**Ruling: ACCEPT rather than rewrite.** Task 4 immediately follows and closes it; plan 4 (unlike plan 3) sets no "every task must type-check" constraint; and splitting differently now would mean rewriting a committed, tested task for a defect whose only real cost is one non-bisectable commit in history. Recording it so the claim in the scan is not left standing as if it held.
**IMPLEMENTER OVERRODE MY PLAN AND WAS RIGHT — kept `Promise.allSettled` where Step 1 said `Promise.all`.** Two reasons, both verified: (1) my "Promise.all is already independent" is true of rejection but NOT latency — `all` still waits for the slowest arm, so the timeout race is needed regardless; (2) **my "fetchRequest never throws" is its INTENT, not airtight.** `fetchRequest` returns `refreshAndRetry(request)` from its catch **without awaiting**; `refreshAndRetry` does `await clearTokens()` from inside its own catch un-wrapped, and `clearOldTokens` calls `SecureStore.deleteItemAsync` with no guard — so that rejection escapes. Under `Promise.all` a country-arm rejection would take the whole configuration down, policies included. **This is a hole in a contract I stated as absolute in the ledger's standing constraints; correcting it there.**
Divergences confined to `getApplicationConfigurationApi`'s body — byte diff against core shows COUNTRY_INFO_TIMEOUT_MS, withTimeout, getProfilePictureByIdApi and getUserProfileApi do not appear in the diff at all. Return type is `ApiResult<NormalizedApplicationConfiguration>` (the brief's stale `ApplicationConfiguration` cannot hold `country: null`); the failure branch is a value check `if (!config.success) return config;` returning the failure member as-is so fetchRequest's message/status survive; core's `throw configResult.reason` becomes a failure ApiResult with status 0, because a throw would sail past the `if (!result.success)` checks Task 4 will write. No casts — the brief's suggested `as CountryInfo` was unnecessary since getCountrySettingsInfo already returns `ApiResult<CountryInfo>`.
Failed country lookup verified three ways (rejection / success:false / 5000ms timeout) — all still return `success: true` with the full payload, and the arm is read only through a `status === "fulfilled" && .success` guard. **The hanging-case test uses fake timers and would time the suite out if `withTimeout` were removed** — mutation-resistant by construction. Absent country reaches the store as `null`, asserted `toBeNull()` in all three loss tests. Exported symbols match core exactly; no logging anywhere in the file, which matters because pos-app ships GlitchTip.
**Carried to Task 4:** (a) `if (!result.success)` is now where `clearGrants()` + `clearGrantedPolicies()` belong — core puts that work in a `catch` that can never fire here; (b) `GrantedPolicies` in `src/actions/AccountService/types.ts` now has no consumer and is the type the AMENDMENT wants widened to `Partial<Record<Policies, boolean>>` — widen or delete it.
Task 3: review **APPROVED** — spec ✅, 0 Critical, 0 blocking Important, 4 Minor. Both departures ruled CORRECT with the escape path traced in pos-app's own source.
**The escape is real, and the scenario is coherent rather than contrived:** a broken SecureStore makes `fetchNewAccessTokenByRefreshToken` fail on its own `getToken("refresh")` — but ITS catch swallows that and returns false; `refreshAndRetry` then throws "Refresh token expired", enters its own catch, and calls `clearTokens()` **a second time against the same broken store, un-wrapped**. One root cause, two calls, only the second escapes. Under `Promise.all` a 401 on the country arm plus a SecureStore fault would take `policies` down with it.
**CORRECTION to what I recorded above (and told the user): the "returns refreshAndRetry without awaiting" detail is a RED HERRING.** The return sits inside the catch clause, so there is no enclosing try left — awaiting would not catch anything either. The escape is real for the reason the code comment gives: the un-wrapped `await clearTokens()` in `refreshAndRetry`'s catch. The source comment is the accurate version; my ledger framing was not.
**My plan's Step 1 was wrong in a SECOND way I had not noticed: its snippet silently dropped `withTimeout` entirely**, passing `getCountrySettingsInfo()` bare into `Promise.all`. That removes core's latency guard, so a hanging country endpoint would hang the login button — an outcome `Promise.all` cannot fix and unrelated to rejection semantics. Keeping allSettled + withTimeout is both safer AND closer to core than my plan would have been.
Corroboration the reviewer found: `SessionProvider.getUserData` ALREADY wraps its Promise.all in try/catch and puts a `.catch()` on its standalone getCountrySettingsInfo call — the existing pos-app author did not trust fetchRequest not to reject either.
Departure 2 confirmed load-bearing: `ApplicationConfiguration.country` is non-nullable, so the brief's type literally cannot carry the absence; the null survives normalizer -> action (`countrySettings?.success ? .data : undefined`, the optional chain also covering the timeout's `undefined`) -> store's `?? state.configuration.country`. The brief's suggested `as CountryInfo` was unnecessary — the response type is a direct alias of the DTO.
Deletions verified safe: all 4 references to the deleted exports are in SessionProvider.tsx, TS emits TS2305 on the two import specifiers only and nothing downstream, and AvatarModal's `getProfilePictureByIdApi` import survives.
**NEW IMPORTANT FOR TASK 4, which the implementer's Concerns did NOT mention:** `SessionProvider.getUserData` has its own `getCountrySettingsInfo()` call at ~line 218. Once Task 4 wires in `getApplicationConfigurationApi`, **every auth path hits country-settings twice**. Harmless at this commit because the new action has no production caller yet — a regression waiting in Task 4.
Minors: a config failure can be delayed up to 5s because allSettled waits for both arms and only the country arm is raced — **inherited from core verbatim, so do NOT diverge here; raise it upstream in core**. Comment density ~31 comment lines to 35 code. Timeout test pins only an upper bound. Mixed jest.mock path styles.
Task 3: complete (commit f25f10a2, approved, no fix round)

## Task 4
Status DONE. Commit c50295ba. Gates: **typecheck 2 errors -> 0** (tree compiles again); lint 0 errors / 35 warnings unchanged; **39 suites / 440 tests all passing** (was 36/422, +3 suites / +18 tests).
**MY BRIEF WAS WRONG AGAIN — defect #13, and the same root as #12.** I wrote that `revokeSessionAuthority` goes on the `!success` branch "not in a catch" because "a catch there would never fire". It DOES fire: `refreshAndRetry` awaits `clearTokens()` from inside its own catch, so a SecureStore failure escapes `fetchRequest` as a rejection and `Promise.all` propagates it — **exactly the escape Task 3's review traced, which I had already recorded and still failed to apply here.** There are **THREE** revoke sites, not two: (1) the `!userResult.success || !configResult.success` branch (core's catch transposed, where ordinary failures land), (2) the real `catch`, kept AND revoking, and (3) the validation gate. Dropping the catch as I implied would have turned that escape into an **unhandled rejection in three callers**.
**Mutation verification was rigorous and the three sites are INDEPENDENTLY covered**: dropping the catch's call fails only "batch rejects outright"; dropping the `!success` branch's fails only the two `!success` tests; dropping the gate's fails only "session does not validate"; dropping either clear inside the helper fails all four. Also mutation-tested: hoisting setConfiguration above the gate, re-adding the country fetch, both clearConfiguration sites, logging `result` instead of `result.message`, adopting a failed refresh, fetching on mount, no-op'ing clearGrantedPolicies, and both currency selectors. **It disclosed a BAD mutation rather than counting it**: its first country-fetch mutation omitted the import so the module failed to load and the whole suite failed — "that proved nothing" — redone faithfully it fails exactly one test.
Selectors: **both `useCountryCode2` and `useCountryName` DELETED** — the post-migration grep returned no hits at all. Added `useCurrencySymbol` in their place (3 consumers). Step 4a discharged.
`Partial<>` surfaced **no** strict-null errors, because pos-app has zero `user.grantedPolicies[key]` read sites — but the agent **proved the widening is load-bearing rather than asserting it**, by reverting it and getting exactly one error: `src/store/user.ts(44,9) TS2345`, `{}` "missing ... 1090 more". `clearGrantedPolicies` cannot exist without it. No casts added.
`GrantedPolicies`: **deleted, not widened** — no consumer, and core's types.ts is exactly the two remaining lines, so deleting REMOVES divergence rather than creating it. Good call.
Duplicate country fetch gone — SessionProvider no longer imports the AdministrationService actions at all, guarded by a test asserting getApplicationConfigurationApi is called once and getCountrySettingsInfo never.
**Four deviations, all following CORE over my brief, all correct:** the gate's last term is `!configuration.user.isAuthenticated` (not `!grantedPolicies`); `setConfiguration` sits BELOW the gate so an unvalidated payload is cleared rather than adopted; **`clearConfiguration()` added to the environment-switch effect** — not in my brief, but core has it and pos-app now persists the same country/settings, so omitting it would have left a real bug (this is the SAME gap plan 3 hit as its Important 1); and `country-settings-storage` added to RETIRED_STORAGE_KEYS, since deleting the store orphans its blob on every existing install (also mirroring plan 3).
Carried: `refresh` still does not revoke on failure, matching core and core's docblock. Fine today — pos-app has no tenant/affiliation switch — but the first one added must close it.
Task 4: review **APPROVED** — spec ✅ on all 8 steps, 0 Critical, 2 Important, 5 Minor.
**The brief-was-wrong override is CONFIRMED in source.** Reviewer traced `refreshAndRetry`'s catch ending in un-wrapped `await clearTokens()` -> `clearOldTokens()` -> `Promise.all` of SecureStore deletes; a keystore failure rejects and escapes `fetchRequest` despite its "never throws" banner, which the reviewer calls **aspirational, not a guarantee**. Removing the catch would produce unhandled rejections in the bootstrap effect and `fetchNewAccessToken`, and a login-button rejection in `signIn`.
**Refinement worth keeping: only the `getUserProfileApi()` arm can actually reject.** The config action already hardens itself with allSettled and converts a rejected config arm to `{success:false,status:0}` — it documents that very escape. So the shipped SessionProvider comment's "either arm's rejection takes the other down" is generous about the config arm. The test correctly drives it through the profile arm.
All three sites reachable, none dead. **Test independence verified BY READING, and it holds** — each test stops at exactly one site, and a fifth test on the `!success` path asserts only userId so it does not blur the mapping. Critically, all four revoke tests call one `expectAuthorityRevoked()` helper asserting configuration.policies/settings/features AND `useUserStore...user?.grantedPolicies` are all `{}` — **so both clears are pinned at every site individually, not merely in aggregate.**
All four deviations confirmed against core@origin/main. On the environment-switch effect the reviewer additionally checked the gap it could leave: it does not call clearGrantedPolicies, but both `setEnvironment` callers sit under `Stack.Protected guard={!user?.userId}`, so no signed-in user's grants can be stranded.
Currency equivalence: **the `||`/`??` trap does NOT apply here** — `ApplicationConfigurationCountry.currency` is non-nullable and every write path coerces (normalizer's `||`, the frozen EMPTY default, and setConfiguration's `??`). Only a corrupted persisted blob could reach Intl with `""`, and hardening `useAppCurrency` would create divergence from core, so leave it.
`Partial<>` load-bearing independently sanity-checked: policies.gen.json has 1094 keys and TS's "1090 more" is exactly 1094 minus the 4 it names. No new casts on any of the 8 touched production files. **No payload or token reaches a log** — two sites log a string message, one logs an Error from SecureStore.
**IMPORTANT 1 — MY WRONG REASONING PROPAGATED INTO A CODE COMMENT.** `ApplicationConfigurationProvider.tsx`'s docblock justifies dropping its catch with "a `catch` here would never fire" because ApiResult — the exact argument this task just refuted. The CONCLUSION is right, but only because `getApplicationConfigurationApi` is hardened with allSettled and cannot reject, NOT because ApiResult implies no catch. As written it states a general rule that a comment 200 lines away proves false.
**IMPORTANT 2:** `country-settings-storage` in RETIRED_STORAGE_KEYS is untested — retiredStorage.test.ts pins only `theme_preference`, so deleting the new entry fails nothing. The otherwise-thorough mutation table skipped it.
Minors: `decodeJWT` is unguarded `atob`+`JSON.parse` called BEFORE the gate outside any try, so a malformed token escapes past all three revoke sites — core-inherited and effectively unreachable in pos-app, **raise upstream rather than diverge**; the `!jwtUser` gate term is correspondingly dead. `clearGrants()` leaves `configuration.user` (previous tenant's email/session id/isAuthenticated:true) — no consumer today but the first must not read it as a liveness signal. **Unreported behaviour change:** the deleted block populated country settings "regardless of whether the profile calls succeed"; with setConfiguration below the gate, a profile failure no longer lands country/currency — benign and arguably more conservative, but it reverses a documented intent and the report should have named it.
Task 4 FIX ROUND: done, commit 68cf0aba. Gates: typecheck 0 errors; lint 0 errors / 35 warnings; **39 suites / 441 tests** (was 440). Controller-verified scope: 3 files, and the ONLY non-comment additions are in retiredStorage.test.ts — both provider files are comment-only.
**The agent added a NEGATIVE CONTROL I did not ask for, and it earns its place.** It extended "leaves live keys alone" to assert `application-configuration-storage` SURVIVES the sweep, because the replacement store sits one entry away in the same list and an over-broad entry there would wipe the persisted country and settings at EVERY BOOT — and it disclosed that this mutation previously failed nothing either. Three mutations now each fail exactly one test (drop the country-settings entry; add the live key to the list; drop theme_preference), so the entries are pinned independently rather than in aggregate.
Provider docblock rewritten to cite the callee's invariant — getApplicationConfigurationApi hardens itself with allSettled and converts a rejected fetchRequest into `{success:false,status:0}` — and it now states in as many words that relaxing that invariant brings the catch back. **That replaces MY false general rule ("ApiResult implies no catch") with the actual reason.** getUserData's catch comment corrected to name the profile arm as the only one that can reject. The unrecorded behaviour change is now in the report.
Ruling: **no scoped re-review.** Task 4 was already APPROVED with both Importants explicitly non-blocking, so this was polish rather than a required fix round, and I verified the scope directly (comment-only + one test file).

## Task 5 (verification)
Status DONE. Commit ec22b68 (AGENTS.md re-measure) + **ef679f6, a controller revert — see below**. Gates MEASURED and re-confirmed after the revert: init silent; typecheck **0 errors**; lint **0 errors / 35 warnings**; **39 suites / 441 tests all passing**.
**3->2 reduction VERIFIED, and stronger than the plan claimed.** One `getApiAbpApplicationConfiguration` hit outside src/saas, inside getApplicationConfigurationApi. At the branch point that endpoint was called **TWICE INDEPENDENTLY** — once by getGrantedPoliciesApi and once by getCurrentUserIdApi, each for a single field — plus getUserProfileApi in the same Promise.all (3), plus getCountrySettingsInfo fired separately outside it. Now the Promise.all is `[getUserProfileApi(), getApplicationConfigurationApi()]` with the country lookup folded inside via allSettled. `getUserData` is the single shared loader for sign-in, bootstrap and refresh (3 call sites), so the reduction is uniform across every auth path.
Mirror parity re-verified: all 5 mirrored files plus __tests__/__fixtures__ diff clean against core's **origin/main** — the agent correctly noted it had to diff against origin/main rather than the local core checkout, which sits on an unrelated branch.
**On-device: NOT RUN, and for the right reason.** The only debuggable pos-app build is on the shared V3, which was **already occupied by a live Metro process (PID 39576) started minutes earlier from the pos-app MAIN checkout, app foregrounded and authenticated**. The agent took ONE passive screencap, sent no input, and stopped — exactly the shared-device-hijack hazard my notes warn about. OrderPAD is not an alternative; pos-app is not installed there. The tag-print country-name path therefore remains unverified on hardware (code-level evidence is a one-line source swap).
GlitchTip audit clean: every reachable console/logger/reportError logs only `.message` strings or caught Errors, never the payload or currentUser. The store has no logging and excludes policies/user from persistence.
**CONTROLLER REVERT ef679f6 — MY BRIEF DEFECT #14, and it is plan 3's Important 3 repeating.** Task 5's prettier pass reformatted `src/store/application-configuration.ts`: 274 lines diverged from core, `create<ApplicationConfigurationStore>()(` split and the whole persist(...) body re-indented. **Root cause is my Task 5 brief**, which said "files mirrored from core are deliberately left unformatted" without noting that THIS file is only PARTIALLY mirrored — core verbatim through line 170, pos-app-only symbols below a marker — so it reasonably read as non-mirrored. The agent's judgement to skip the brief's literal blanket prettier command was otherwise sound; it just misclassified this one file.
Reverted the file to 68cf0ab. Verified three ways: plain diff (CRLF artifact only), `diff --strip-trailing-cr` clean, and **hash of CR-normalised content identical to core's at 8e8c3bf0**. pos-app additions below the marker intact. Gates re-run green afterwards. I did this myself rather than dispatching, because it is a mechanical restore to a previously-approved state that is fully verifiable by hash.

## FINAL WHOLE-BRANCH REVIEW (opus, 792f4e2..ef679f6, two passes)
Verdict **Ready to merge: WITH FIXES.** 0 Critical, 3 Important — **all documentation/comment level; nothing in the runtime path is wrong.**
Verified: all 9 src/config files byte-identical to core; `types.ts` byte-identical too (deleting GrantedPolicies REMOVED divergence); the store's mirrored region hashes identical to core's whole file after my revert, marker at 172, pos-app block intact; the ApiResult adaptation is a minimal faithful transposition with only the two failure branches and the country unwrap differing; all three revoke sites independently pinned with BOTH clears asserted at each; orphaned storage retired and pinned three ways including the negative control; zero dangling references.
**IMPORTANT 1 — a sharp catch about ME.** AGENTS.md's new mirroring guardrail lists only `src/config/*` and omits `src/store/application-configuration.ts` — **the exact file that broke.** I diagnosed the root cause correctly ("the brief didn't say this file is only PARTIALLY mirrored") and then wrote the durable artefact without saying it either. Fix: add the store and state the marker convention explicitly.
**IMPORTANT 2 — the verification recipe misfires, and I used the same flawed method myself.** `core.autocrlf=true` here: the store and user.ts are **CRLF in the working tree, LF in the committed blob**; the src/config files are LF on disk only because agents wrote them rather than git checking them out, so a fresh clone makes them ALL CRLF. AGENTS.md's documented `diff core/... pos-app/...` therefore reports full-file divergence that is pure line-ending artefact — the reviewer hit it, and notes my `grep -c $'\r'` guard **silently no-ops under sh**. That is precisely the confusion I ran into after the revert. Fix: diff BLOBS (`git show HEAD:<path>` vs `git -C ../core show origin/main:<path>`) or add `--strip-trailing-cr`.
**IMPORTANT 3 — a divergence bought for a claim that is false here.** `src/store/user.ts` differs from core by one reworded docblock saying "This is the map policy gates read, so it is what fails permission-gated UI closed" — but **pos-app has ZERO production readers of `user.grantedPolicies`**; all 9 non-test hits are writes, types or comments. Core's original wording ("policy gates *in derived apps* read — core has none of its own") is both TRUE here and would leave the file byte-identical. Reverting is strictly better on both axes.
**Honest caveat the reviewer insisted on, and it is right:** in pos-app TODAY this is entirely forward-looking. No readers of grantedPolicies, none of useSettingFlag/useEarlyRefundAvailable, and no in-session tenant switch (`setTenantId` only feeds the pre-login `__tenant` header). The work is correct; the present-tense risk is not yet real here.
Minors: user.test.ts is a REWRITE of core's rather than a mirror and drops core's assertion that `userName` survives the clear — same future conflict, no benefit; `jest-setup.ts:5` still lists country-settings among persisted stores; the mirrored store docblock says "both failure paths" where pos-app now has three (**inside the mirrored region — do NOT edit, note for the upstream core fix**); getCurrencySymbol/DEFAULT_CURRENCY_CODE are exported with no external consumer, which is the same rule Step 4a used to delete the two selectors, applied unevenly; decodeJWT unguarded before the gate (upstream); the provider's "cannot reject" invariant does not cover the normalizer throwing on a malformed 200 body; and revoking on the ordinary `!success` path has no retry, so the first policy gate pos-app adds will blank on a transient blip until the next successful getUserData.
Rulings all upheld: the non-compiling intermediate, the allSettled override (independently confirmed at customFetch.ts:114), three revoke sites, both selectors deleted, GrantedPolicies deleted rather than widened, no re-review of the Task 4 fix round, and skipping the device check rather than hijacking a live shared session — with the note that the print path should be the first thing checked post-merge.

## ON-DEVICE VERIFICATION — RUN, AND IT PASSED (2026-09-08, post-merge)
Previously reported NOT RUN. User authorised taking the shared device and supplied the test credentials in C:/unirefund/AGENTS.md. **Gap now CLOSED.**
Setup: the installed build could NOT load Metro — package exposes only `.MainActivity`, no dev-launcher, so it is a standalone debuggable build with an embedded bundle. A deep link was accepted by the running instance but Metro served ZERO bundle requests. Also, the earlier "live Metro from another session" was gone (nothing listening on 8081-8083), so nothing was actually being hijacked. Built and installed a fresh debug build from the merged worktree (`npx expo prebuild` + `npx expo run:android --device V3` — note `--device` wants the NAME `V3`, not the serial `VA08263HJ1120`, which errors "Could not find device"). Confirmed running from Metro by the LogBox "Open debugger to view warnings" banner. Generated `android/` is gitignored, so the tree stayed clean.
Login: DEV env, tenant **Faroe Islands**, `siggi.merchant`. Password was already pre-filled on this device (7 chars) — contrary to the pos-app memory note that the form is never pre-filled, which was recorded on the departed Sunmi P2.
**RESULT 1 — currency, unplanned but decisive.** The Sale screen renders **DKK 0,00** and the tag detail shows every amount in DKK. `useAppCurrency()` reads `configuration.country.currency` from the NEW store; a broken path would show the `TRY` default. So the country lookup landed, `setConfiguration` kept it rather than overwriting, and the currency selector reads it — end-to-end on real hardware against the real backend.
**RESULT 2 — the print template, the actual outstanding gap. PASSED.** Tapped Print on tag FRO2026090400001 and captured ReactNativeJS logcat:
  `[Sunmi] Item 2/61: text "Faroe Islands"`
  `[Sunmi] Item 8/61: text "by Unirefund Faroe Islands"`
  `[Sunmi] Item 24/61: text "the Faroe Islands Retail Expor"`
Item 8 is literally the template's `issued by Unirefund ${countryName}` line. So `countryName` arrives as "Faroe Islands" via `getApplicationConfigurationApi` -> normalizeApplicationConfiguration -> `configuration.country.countryName` -> tagPrintTemplateV3. **Had the migrated read been wrong it would print "by Unirefund " with an empty string** — the old country-settings store is deleted, so there is no fallback that could mask it.
Print completed cleanly: `exitPrinterBuffer done`, `Sunmi print done`, `print() resolved successfully`. 61 items, no errors.
Note: an unrelated toast appeared after login — "Error: Failed to start the tr..." (truncated), on the Sale screen. Not connected to the configuration path; worth a separate look.
**Every claim in plan 4's PR body is now verified, including the one that shipped unverified.**
