# SDD ledger — plan: /c/unirefund/docs/superpowers/plans/2026-09-07-app-config-provider-5-remove-adapters.md

Spec: /c/unirefund/docs/superpowers/specs/2026-09-04-application-configuration-provider-design.md (read, reachable)

## Workspace

- Worktree: `C:/unirefund/.worktrees/web-app-app-config` on branch `feat/app-config-remove-adapters`, stacked on `feat/app-config-provider` (plan 1) at `34e76e8fa`.
- Submodule `packages/utils` (`web-utils`) on branch `feat/app-config-provider` — the same branch plan 1 raised as PR #48. Commits here extend that PR's branch.
- Plan 1's PRs: `ayasofyazilim-clomerce/web-utils#48` and `unirefund-web#294`. **#48 must merge before this branch's submodule pointer resolves for anyone else.**

## Origin of this plan

The user asked for the deprecated providers to be removed outright, reversing
plan 1's approved facade decision. Recorded in the spec's out-of-scope section
as an explicit reversal rather than a silent change.

## Baseline (measured on this branch's base, 34e76e8fa)

| Gate | Command | Baseline |
| --- | --- | --- |
| Type-check (web) | `cd apps/web && pnpm run type-check` | **0 errors** |
| Type-check (ssr) | `cd apps/ssr && pnpm run type-check` | **0 errors** |
| Unit tests | `cd apps/web && pnpm run test:unit` | **136 tests / 135 pass / 1 pre-existing skip** |
| Lint | `pnpm run lint` (root) | exit 0, **0 errors**, 948 warnings (web 465, ssr 459, ayasofyazilim-ui 14, ui 10) |
| Build | `pnpm run build` (root) | **2 successful, 2 total** |

The type-check baseline is genuinely 0, not 2: `next-env.d.ts` is gitignored and
generated by `next build`, and it declares `*.png`/`*.svg` imports. This worktree
has been built, so the phantom `TS2307`s are gone. Do not accept non-zero as
pre-existing.

## Consumer inventory (measured 2026-09-07)

`useTenant` — 122 files:

| Fields destructured | Files |
| --- | --- |
| `localization` only | **109** |
| `countryCode2` only | 5 |
| `formatToTenantDate` + `localization` | 2 |
| non-destructured | 2 |
| `tenantName` | 1 |
| `tenantId` | 1 |
| `formatToTenantDate` | 1 |
| `currency` + `localization` | 1 |

`useGrantedPolicies` — 145 files, all of shape `const { grantedPolicies } = useGrantedPolicies()`.

This shape is why the migration is mechanical: 109 files swap to a hook named
for what they already use, and all 145 policy consumers migrate by
rename-on-destructure (`const { policies: grantedPolicies } = ...`), leaving
every `isActionGranted(...)` call untouched. 254 of 267 files are one-line edits.

## Rulings

- **Ruling 1: merge plan 5's Tasks 1 and 2.** My plan made `lang` a REQUIRED prop on `ApplicationConfigurationProvider` in Task 1 and wired it at the two mount sites in Task 2. That guarantees a non-compiling intermediate — Task 1 landed with exactly 1 type error per app, both the identical missing-`lang` error at the sole mount site. The implementer correctly declined to fix them as out of scope, which is right. But a task that leaves the tree non-compiling cannot carry its own test cycle, and its review can verify no gate; this is the same defect I ruled on for core's Tasks 4+5, and I repeated it here. Decision: resume the same implementer for Task 2 (two lines, in files it just reasoned about), then review Tasks 1+2 as one combined range. Patch the plan document so a future run does not hit the same broken intermediate. — Cost if wrong: a slightly larger combined review surface; the alternative is a knowingly-broken commit and a meaningless review.

## Progress

Task 1: implementer DONE_WITH_CONCERNS — submodule `400ba1c`, superproject `753d236`. Created `app-config/localization.ts` (the `Localization` type, `countryToLocale`, `getLocaleFromCountryCode` moved unchanged from `tenant.tsx`), added `lang` + `LangContext` + `useLocalization` + the two `localStorage` writes to `provider.tsx`, re-exported the pure module from `logic.ts`, and added 4 tests for the locale mapping. test:unit 140 tests / 139 pass / 1 skip / 0 fail; lint unchanged at 0 errors / 948 warnings. Type-check shows 1 expected error per app pending Task 2.
Task 2 (merged into Task 1 per Ruling 1): implementer DONE — superproject `6817c4589`, no submodule change. All four gates now FULLY GREEN: apps/web type-check 0 errors, apps/ssr type-check 0 errors, test:unit 140 / 139 pass / 1 pre-existing skip / 0 fail, root lint exit 0 / 0 errors / 948 warnings unchanged. The merge achieved its purpose — the tree compiles again.
Task 2: one addition beyond the brief — apps/ssr's `Providers` did not destructure `lang` at all, so the implementer added it. Worth noting the history: plan 1's final fix wave deliberately REMOVED `lang` from that same destructure, because its only consumer there (`getResourceData`) had just been deleted and leaving it bound-but-unused added a lint warning above the expected count. It is now live again. Not a contradiction, just sequencing — passed to the reviewer with that context so it can judge coherence rather than churn.
Tasks 1+2: review package `review-34e76e8fa..6817c4589.diff` (augmented to 249 lines, 7 files across both repos). Reviewer dispatched. Named risk given: whether `useLocalization` returns EXACTLY the shape `useTenant().localization` returned, since Task 3 points 109 files at it — a missing or renamed field would surface as 109 type errors then rather than now.
NOTE: recreated `augment-package.sh` in this workspace. Plan 1's copy died with its workspace, and without it every review of a submodule-touching task sees only the pointer bump.
Tasks 1+2: review APPROVED — Spec ✅, 0 Critical, 0 Important, 2 Minor. **The named risk is CLOSED:** reviewer read apps/web/src/providers/tenant.tsx directly and confirmed `useTenant().localization` is exactly `{ locale, timeZone, lang }`, matching the new `Localization` interface and `useLocalization()`'s return field-for-field. That is the precondition for Task 3 pointing 109 files at it. Also verified both preserved quirks byte-identical ("en-UK", `|| "UK"`, `|| "us"`, `.toLocaleLowerCase()`), purity of localization.ts and logic.ts, that the index.tsx barrel already re-exports useLocalization so nothing more is needed before Task 3, and that the new memo/effect dep arrays are strictly more precise than the coarse `[config, lang]` they replace.
Tasks 1+2: **another plan inaccuracy of mine, caught and fixed by the implementer** — my Task 2 brief said "Both `Providers` components already receive `lang`". False for apps/ssr: `ProvidersProps` declared `lang: string` but the function body destructured only `{ children }`, so `lang={lang}` would not have compiled. The implementer added the binding; the reviewer confirmed it necessary and minimal, and specifically that it did NOT resurrect the previously-deleted `getResourceData` call that once consumed `lang` there.
Tasks 1+2: minor (deferred): a plan-inherited trailing comma in the new localStorage call violates the repo's `"trailingComma": "es5"` prettier setting (cosmetic, eslint does not catch it, resolves on the next prettier pass of that file); and the new module-private `LangContext` lacks the doc comment its `tenant.tsx` analogue had.
Tasks 1+2: **open verification item carried forward** — provider.tsx is JSX and the runner is .ts-only, so NOTHING executable exercises useLocalization's runtime behaviour, the localStorage effect, or either mount site. In particular, apps/web will briefly have TWO independent LangContext objects (tenant.tsx's and the new one) holding the same value, and the two providers' effects write the same two localStorage keys in the same tick. Asserted harmless by design; unexercised by any gate. First real check is whichever later task mounts a useLocalization consumer, and ultimately the production build.
Tasks 1+2: complete (commits 34e76e8fa..6817c4589, review clean, no fix round needed)
Task 3: dispatched — the 109-file batch.
Task 3: review APPROVED — Spec ✅, 0 Critical, 0 Important, 2 Minor. Strongest verification in the series: the reviewer reproduced the entire 109-file set from BASE-COMMIT CONTENT independently (via `git show 6817c4589:<path>` across all 122 useTenant files) and diffed it against the actual changed-file list — zero lines of difference. It then hunted the classifier regex's blind spot on purpose and found `phone-with-type.tsx`'s non-destructuring `const tenant = useTenant()` form, confirming it was correctly excluded. It traced the full import chain (packages/utils/package.json wildcard `"./*": "./*/index.tsx"` → app-config/index.tsx → `export * from "./provider"` → provider.tsx:60 defines useLocalization) rather than accepting "0 errors". And it checked for orphaned imports by counting `localization` occurrences per file — minimum 2 across all 109, so none declares it unused.
Task 3: **my brief's speculation disproved** — I suggested the two prior import paths (`@/providers/tenant` ×92, `@/src/providers/tenant` ×17) might signal an ssr/web split or different tsconfig. All 17 are in apps/web too; both aliases resolve to the same physical file, one via the specific `@/providers/*` rule and one via the catch-all `@/*`. Pre-existing inconsistent authoring, nothing structural.
Task 3: **correction to my own gate framing** — apps/ssr has ZERO useTenant consumers of any kind, so citing "apps/ssr type-check 0 errors" as evidence for a useTenant-touching change carries no evidentiary weight. Applies to Task 4's report too.
Task 3: complete (commits 6817c4589..e3cbaa817, 109 files, review clean, no fix round)
Task 4: dispatched — the remaining 13 useTenant consumers plus moving the date formatters into the submodule.
Task 4: implementer DONE — submodule `5a51cf5` (useTenantInfo, useCountryInfo, useTenantDateFormatters), superproject `ca3b8753118` (12 consumers + pointer). All gates green: type-check 0 errors, test:unit 140/139/1/0, lint exit 0 / 0 errors / 948 warnings unchanged. **Zero useTenant consumers remain** — the only surviving reference is its own `export const useTenant =` line in providers/tenant.tsx.
Task 4: **inventory correction, my error.** My classifier used `useTenant\b`, which matched `providers/tenant.tsx` itself — the hook's DEFINITION, not a consumer. So the "13 remaining" was really 12, and the total was 121 real consumers (109 + 12), not 122. The 109 figure was unaffected, since tenant.tsx never calls `const { localization } = useTenant()`. Every "122" and "13" in my earlier reporting was off by that one file.
Task 4: **cost of my mid-task requirement change.** The user asked for flat accessor hooks (`const { tenantId } = useTenantInfo()`) instead of chained access while this task was already running. 9 of the 12 files had to be reworked. The agent spent 132 tool calls / ~19 min on this task, materially more than the 109-file batch, almost entirely because of the late redirect. Catching a call-site convention BEFORE dispatching the task that establishes it is cheaper than after — the same lesson as the two task-splitting rulings.
Task 4: implementer flagged but did NOT act on: `useTenantInfo().isHost` could replace sidebar-layout.tsx's `isHostTenant(tenantId)` call. Left for a separate decision, correctly — swapping a computed predicate for a contract field is a behaviour question, and isHostTenant has server-side callers.
Task 4: review APPROVED — Spec ✅, 0 Critical, 0 Important, 1 Minor (a style split: useLocalization reads the context directly while the two new hooks go through the useApplicationConfiguration wrapper — functionally identical). Reviewer independently reproduced both claims by grep: exactly one `useTenant` reference remains (its own definition at tenant.tsx:17) and zero chained `useApplicationConfiguration().tenant|.country` accesses anywhere. It verified the new hooks field-for-field against the still-resident original, confirmed `currency` correctly has no coalesce (types.ts:31 makes it the only non-nullable field in ApplicationConfigurationCountry), and confirmed the date-formatter semantics by line-by-line comparison — noting explicitly that type-check CANNOT distinguish a swap, so that textual comparison is what rules it out.
Task 4: complete (commits e3cbaa817..ca3b87531, 12 consumers + 3 new hooks, review clean, no fix round)
**useTenant is now fully unconsumed** — 121 real consumers migrated across Tasks 3 and 4 (109 + 12). Only its own definition survives, which Task 6 deletes.
Task 5: dispatched — the 145 useGrantedPolicies consumers, the last batch.
Task 5: **Ruling: fix the CONTRACT type, not the call sites.** The user objected to `useApplicationConfiguration() as { policies: Policies }` at a call site, and they are right — the cause is a weak contract type, not an inconvenience. Verified: app-config/types.ts:48 declares `policies: Record<string, boolean>` while isActionGranted expects `Policies | undefined` = `Record<Policy, boolean>` over policies.json's 1094 keys, and the payload's `grantedPolicies` is the GRANTED SUBSET (`{[key:string]: boolean} | null`), so it was never a complete map. The old adapter hid that with one cast (granted-policies.tsx:12); publishing the weak type just pushed the same cast out to 145 consumers. Decision: type the contract `policies: Policies` (importing from ../policies/types — safe, that module imports only policies.json, no cycle) and keep ONE cast in normalize.ts at the boundary where untyped ABP JSON enters the typed domain, with a comment explaining the partial-vs-full key space. — Cost if wrong: the contract claims key completeness the data lacks; mitigated because isActionGranted reads via `?.[policy]` so a missing key is correctly falsy, and this is exactly the typing all 145 consumers have been operating under since the adapter cast for them.
Task 5: **note — this is NOT the redundant cast a prior review had me remove.** That one asserted what the compiler already knew (`grantedPolicies` was already `{[key:string]: boolean}`). This one asserts what the compiler cannot know: that a partial map may stand for the full key space. One honest cast at a trust boundary is correct engineering; 145 at consumers is not.
Task 5: an independent reviewer had already flagged this in plan 1's final review as Minor 3 — "the new surface has no policy accessor, and weaker key typing than the one it replaces... the template is publishing the weaker surface as the migration target." I deferred it then. The user reaching the same conclusion from a call site is a good signal the deferral was wrong.
Task 5: **parity deferred deliberately.** core/pos-app/super-app also declare `policies: Record<string, boolean>` in their appConfigTypes. Not changing core now: its PR #22 is open and mid-review, its earlier commits are already on main, and a prior review established nothing in core reads store policies yet — so the weak typing is currently harmless there. Recording it as a follow-up rather than reopening a PR under review. — Cost if wrong: the four repos' contracts differ in one field's typing until someone aligns them; tsc catches any real mis-port.
Task 5: implementer DONE — submodule `04ecf0e` (contract typed `policies: Policies`, one boundary cast in normalize.ts), superproject `768a2d932` (145 consumers + normalize.test.ts ripple + pointer). **No cast at any call site** — the contract fix did exactly what it was supposed to. The implementer had briefly applied the brief's sanctioned per-site cast to 2 files and reverted both before the contract change, which is the right order.
Task 5: controller's own distinct-line verification over the apps/ portion — 148 call swaps, 145 new imports, 138 plain isActionGranted imports, 6 co-import variants preserving Policy/Policies, 6 multi-line import fragments removed, ZERO casts. Arithmetic reconciles (145 files, 148 calls because three files hold multiple components). Semantic drift within edited lines is mechanically impossible.
Task 5: the one non-consumer change is a test SHARPENING, not a weakening. `config.policies["Nope.Missing"]` (a fabricated key the stricter type no longer permits) became `config.policies.AbpIdentity` — a REAL policy key the fixture never grants — with a comment. The new assertion proves a real-but-ungranted key reads falsy, which is precisely the fail-closed semantic that matters; the old one only proved a nonexistent key does. Passed to the reviewer for an independent read rather than asserted.
Task 5: review APPROVED — Spec ✅, 0 Critical, 0 Important, 1 Minor. Reviewer independently re-derived set membership with `git grep -l` at the base SHA (exactly 145, `comm -3` showing normalize.test.ts as the only non-overlap), verified all 6 co-import variants keep their `type` qualifiers unflipped against the base import blocks, confirmed both ssr consumers migrated, and confirmed granted-policies.tsx untouched and still compiling with zero consumers. It independently agreed the test change STRENGTHENS: "Nope.Missing" proved only that an absent key reads undefined — true of any object — whereas `AbpIdentity` is a real member of the 1094-key union the fixture never grants, so it actually exercises the partial-map/full-key-space distinction the contract change rests on. It also noted the fix was load-bearing, not cosmetic: under the widened type, indexing with an arbitrary string is a compile error (TS7053).
Task 5: reviewer found a SECOND cast I had not asked for — on `EMPTY_APPLICATION_CONFIGURATION` at normalize.ts:36 — and judged it correct: the same trust-boundary argument applied to "no data". Agreed.
Task 5: minor (to fold into Task 6): `Policies = Record<Policy, boolean>` structurally asserts full coverage over a map that is only ever partial at runtime. Today every consumer goes through isActionGranted/isUnauthorized, which read defensively via `?.[policy]` + `!`, and the reviewer grepped all 145 plus both cast sites for direct indexing that bypasses those helpers and found none. So the gap is inert — but inert BY CONVENTION, not by the type system. A doc note on the `policies` field naming the granted-subset semantics would make it discoverable to the next person who reaches for `config.policies.SomeKey` directly.
Task 5: complete (commits ca3b87531..768a2d932, 145 consumers + contract fix, review clean, no fix round)
**ALL 267 CONSUMERS MIGRATED** — 121 off useTenant (Tasks 3-4), 145 off useGrantedPolicies (Task 5), one file in both. Both hooks now have zero consumers.
Task 6: dispatched — the deletions.
Task 6: implementer DONE_WITH_CONCERNS — submodule `9a6686991b0c`, superproject `c781b4213ae6`. All FIVE gates green: apps/web type-check 0, apps/ssr type-check 0, test:unit 140/139/1/0, root lint exit 0 / 0 errors / 948 warnings (unchanged — the deleted files carried no warnings of their own, verified with a forced non-cached run), and **root build 2 successful / 2 total, both "Compiled successfully"**. Deleted: apps/web providers/tenant.tsx, apps/web providers/configuration.tsx, apps/ssr providers/configuration.tsx, packages/utils policies/granted-policies.tsx (+ its re-export). Kept deliberately: policies.json, isActionGranted, isUnauthorized, and the ayasofyazilim-ui MasterDataGrid provider itself (only its mount was removed).
Task 6: **the Step-1 gate I designed was INCOMPLETE — my defect.** My "prove zero consumers" grep matched exported SYMBOL names (useTenant, TenantProvider, ConfigProvider, …), so three `refund-filters` files doing `import type { Localization } from "@/providers/tenant"` were invisible to it — a type-only import of a type, not a hook. It surfaced as a type error only AFTER the deletion, and was fixed by repointing to the structurally-identical `Localization` at @repo/utils/app-config. **Lesson: to prove a module is safe to delete, grep the import PATH, not its exported symbols.** Controller re-verified by path afterwards: `grep -rn "providers/tenant\|providers/configuration\|policies/granted-policies"` returns nothing.
Task 6: Step-2 MasterDataGrid check came back clean — the single flagged line was providers.tsx itself, a substring false-positive on `<MasterDataGridResourcesProvider`. All 97 real consumer files supply their own `t`, so removing the mount cannot silently strip grid labels.
Task 6: **a `next dev --turbopack` server was already running in this worktree on port 3001 (PID 7620)**, started earlier by something outside this session. The implementer could not stop it (taskkill denied as destructive) and correctly did NOT build into the live `.next` — my own recorded hazard is that `next build` over a running dev server replaces its chunks and sends the browser into a ChunkLoadError reload loop. It built to a temporary distDir via a next.config.js tweak it then reverted (tree verified clean). The build gate is trustworthy; the dev server is still live with a `.next` now stale against four deleted files, and needs manual attention.
Task 6: review APPROVED — Spec ✅, 0 Critical, 0 Important, 2 Minor (both pre-existing prose/typing, untouched by the diff). Reviewer settled both disclosed concerns properly. (a) The `Localization` repointing is not merely structurally identical but the SAME TYPE FLOWING AT RUNTIME: it traced client-page.tsx:81 calling useLocalization() and passing the result through RefundFilters → TagsPanel → RefundTagList, exactly the three repointed files. (b) The alternate-distDir build is TRUSTWORTHY: distDir only relocates output and does not alter module resolution, tsconfig paths, bundling, or "use client"/"use server" boundary analysis — the mechanisms the gate exists to exercise. It also noted the production build script is `next build`/`next build --webpack` while the dev server was `--turbopack`, so that bundler mismatch is pre-existing repo behaviour, not introduced by the workaround, and that a cold `.next-gatecheck` made the run STRICTER than a warm incremental build.
Task 6: complete (commits 768a2d932..c781b4213, review clean, no fix round)
Task 7 (verification) — run by the controller directly, since it modifies no files and its deliverable is greps. **Ruling: no task review for Task 7**, same as plan 1's: an empty diff gives a reviewer nothing to verdict, and the final whole-branch review covers the branch regardless.
Task 7 results — ALL CLEAN: (1) zero CODE references to useTenant/TenantProvider/useGrantedPolicies/GrantedPoliciesProvider/@deprecated anywhere in apps or packages/utils; the only 4 remaining mentions are PROSE in doc comments explaining history. (2) ApplicationConfigurationProvider mounted exactly once per app, both with `configuration` and `lang`. (3) MasterDataGridResourcesProvider unmounted in both apps and still present in ayasofyazilim-ui as library API. (4) policies.json, action-policy.tsx, utils.ts, types.ts all survive. (5) Branch: 6 commits, 200 files changed, +744/-738.
Task 7: **one of those prose lines carries my own off-by-one** — provider.tsx:57 says "109 of the 122 former useTenant consumers", but 122 counted providers/tenant.tsx itself (the definition). The real figure is 121 consumers (109 + 12). Folding into the final-review fix wave rather than fixing in the controller session.
FIX-WAVE ITEM (found by the controller while writing plan 6, BEFORE the final review reported): `packages/utils/api/action.ts:14` returns `config.policies as Policies` — after Task 3's contract fix (04ecf0e) `config.policies` is ALREADY typed `Policies`, so this cast is now a NO-OP left over from before the contract change. It is exactly the redundant `as` the user objected to. Fix: `return config.policies;` and drop the now-unused `import { Policies }` on line 3 (which is also a value-import of a type-only export). The other two `as Policies` sites (normalize.ts:36 and :112) are the LEGITIMATE documented trust boundaries and must stay.
FIX-WAVE ITEM: 4 stale prose references to the deleted providers in doc comments — is-host-tenant.ts:14, localization.ts:16, provider.tsx:57, provider.tsx:76. provider.tsx:57 additionally carries my own off-by-one ("122 former useTenant consumers" should be 121; 122 counted providers/tenant.tsx itself).
FIX-WAVE ITEM (IMPORTANT, controller-found): `packages/utils/app-config/fetch.ts` lines 43-50 quote request counts the SPEC explicitly forbids. The spec was amended to say "The per-render request count is unmeasured. Do not quote one." after BOTH my "~128 per render" and its replacement "2 typical / 11 worst" turned out unverified. The doc comment still asserts "a typical route made 2 application-configuration requests ... and now makes 1; the worst route, operations/tax-free-tags/[tagId]/page.tsx with 9 call sites of its own, made 11 and now makes 1", plus "Of 135 isUnauthorized(...) call sites only 8 pass grantedPolicies" and the per-call-site "19 KB / ~225 ms round-trip" attribution. The 19 KB / ~225 ms payload figure IS measured and may stay; the per-render REQUEST COUNTS must go, to match the spec. Rewrite the cache() rationale to say cache() is load-bearing because most isUnauthorized call sites do not pass grantedPolicies and would each otherwise issue their own round-trip, without asserting a per-route count.

## FINAL WHOLE-BRANCH REVIEW (opus, range 34e76e8fa..c781b4213 + submodule 1f2dce8..9a66869)
Verdict: **With fixes.** 0 Critical, 3 Important, 10 Minor. Independently reconciled the mechanical batch (137+6+1+1=145 policy-import removals; 148 call swaps vs 145 files, 3 extra = multi-component files; 96+25=121 tenant-import removals, confirming my off-by-one correction; ZERO `as` additions; the only blank-line deletions in 10,334 lines are inside the 3 deleted files).
Reviewer CLOSED two of my open "no test reaches this" caveats by other means: (1) `MasterDataGrid`'s `config.localization` is typed `Localization` (master-data-grid/types.ts:16,374), so tsc DOES structurally validate useLocalization()'s return at ~100 grid call sites, field for field. (2) `useMasterDataGridResources()` has exactly ONE call site in the whole library (master-data-grid.tsx:79); every sub-component takes `t` as a prop — so the unmount is safe for a stronger reason than the plan gave. (3) It also verified `resolveJsonModule: true` in packages/typescript-config/base.json, WITHOUT which `Policy` would degrade to `string|number|symbol` and the contract change would have achieved nothing.
**RULING CORRECTED BY REVIEWER — I accept it.** My ruling said publishing the weak type "pushed the same cast out to 145 consumers". FALSE: `policies/action-policy.tsx` carries `// @ts-nocheck` AND references `Policies` without importing it, so under @ts-nocheck that identifier is `any` and `isActionGranted`'s 2nd param accepted `Record<string,boolean>` silently. Only **2** of 145 ever needed a cast (affiliations/table.tsx:218, sidebar-template/index.tsx:65) — exactly matching the ledger's own note that the implementer applied 2 casts and reverted them. Overstated ~72x. The decision was still right, for a different reason: the barrel published a strictly weaker type than `isActionGranted` documents, and `Policies` buys real typo protection over 1,094 keys. **This matters beyond bookkeeping: the "145 casts" framing is what would have justified the same move in plans 3/4/6, and it would not hold there either.**
Reviewer AGREED with: merging Tasks 1+2 (non-compiling intermediate); no task review for Task 7; the alternate-distDir build being trustworthy; deferring code parity for core/pos-app/super-app; and the 121-not-122 correction (independently reproduced).
Reviewer EXTENDED my Task-6 lesson: my corrected grep ("grep the import PATH, not the symbols") was still CODE-ONLY. `AGENTS.md:116` is the same defect one file type over. Durable rule: **to prove a module is safe to delete, grep its path across EVERY text file, including instruction and doc files.**
NEW FINDING recorded, no code change (reviewer verified inert): `ApplicationConfigurationProvider` now runs the two localStorage writes in **apps/ssr**, which never wrote them — including its `(public)` layout, so ANONYMOUS visitors get countryCode2="us"/tenantTimeZone="UTC". Inert today because the only readers (packages/ui address-combobox.tsx:72, ayasofyazilim-ui date-picker/index.tsx:93) are unreachable from apps/ssr. A future ssr date field would inherit it. -> goes in the PR description.
Ruling: **land `Partial<Policies>` now rather than tracking it.** It keeps the typo protection (an unknown key is still an error), drops the false claim of 1,094 required keys, and — decisive — should REMOVE all three remaining `as Policies` casts rather than relocate them, which is precisely what the user objected to. Cost is contained (~5 files, no call-site churn). Doing it before plans 3/4/6 copy the weaker type outward.
Ruling: **DROP `formatToTimezoneDate`** rather than document it. It had zero consumers at base too, so it is dead surface this branch carried forward — on a branch whose purpose is deleting dead surface. Nothing consumes it, so removal is free.
Ruling: **fix the 78 duplicate-import files.** Mechanical and zero-risk; no lint rule catches it, and leaving 78 files with two imports from the same module reads as unreviewed. NOT enabling `import/no-duplicates` (would surface pre-existing repo-wide warnings and move the 948 baseline).
Deferred minors declined: L80 style split (cosmetic), Minor 10 (same), Minor 13 (`Localization` declared twice — de-duplicating would put a UI import into the "pure" logic.ts that fetch.ts's docblock leans on).
Reviewer also flagged L67a as OVERSTATED by me: the submodule ships NO prettier config, standalone prettier defaults trailingComma "all", and base provider.tsx:10 already had one. Changing it would make the file inconsistent. Can stand.
FINAL FIX WAVE: DONE_WITH_CONCERNS. submodule 55cd2ab2, superproject f8208705 (incl. pointer bump). Gates flat: web tsc 0, ssr tsc 0, packages/utils tsc 0 (agent ADDED this as a 4th gate — good, the submodule was never independently type-checked before), test:unit 140/139/1 skip, lint exit 0 at EXACTLY 948 warnings (14+10+459+465), measured pre-edit rather than taken on faith.
ALL THREE `as Policies` CASTS REMOVED — none survived, none added; repo-wide grep for `as Policies`/`as Policy`/`as Partial<Policies>` returns nothing. This fully discharges the user's "i didnt like the as part" objection AT THE CONTRACT rather than relocating it.
Agent found things the review and my brief both missed: (1) a THIRD boundary prop in `contracts/_components/utils.tsx` spelling the type inline as `Record<Policy, boolean>` — invisible to a `Policies` grep, surfaced only as 2 tsc errors; (2) a dead `Policies` import in normalize.ts my brief did not flag; (3) a test comment in normalize.test.ts whose premise A1 falsified.
MY BRIEF HAD TWO ERRORS, both caught: the affiliations table is under `(unirefund)/parties/…` not `(management)/parties/…`, and the duplicate-import count is **77, not 78** (I passed the reviewer's figure through without re-counting — the same "don't quote an unverified number" failure this project keeps re-teaching me). 73 of the 77 wrap to two lines because the merged single-line form is 86 chars against printWidth 80; agent verified against two pre-existing repo precedents.
CONCERN 1 (controller must act): Part B2's spec edit is APPLIED BUT UNCOMMITTED. `C:/unirefund/docs` is a THIRD git repo, and it already held ANOTHER SESSION'S WIP (modified template-repo-findings.md + an untracked plan file). Agent edited only the spec (+23/-3) and stopped rather than sweep foreign work into a commit — exactly right, and exactly the shared-checkout hazard. Controller commits that one file, path-scoped.
CONCERN 2 (follow-up, out of scope): `isActionGranted`'s 2nd parameter is effectively `any` — `action-policy.tsx` annotates it `Policies` WITHOUT importing it, and its `@ts-nocheck` degrades the unresolved name to `any`. That is the ROOT CAUSE of why the weak contract survived review for so long, and it means the Partial<Policies> win stops at the boundary props. Worth its own ticket.
Agent verified my brief's premises rather than assuming: policies.json really has 1,094 keys, and formatToTimezoneDate really had zero consumers pre-branch (all 4 merge-base hits were its own definition/wiring inside the deleted tenant.tsx).
