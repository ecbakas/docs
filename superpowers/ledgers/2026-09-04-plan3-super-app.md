# SDD ledger — plan: /c/unirefund/docs/superpowers/plans/2026-09-04-app-config-provider-3-super-app.md

Repo: C:/unirefund/super-app, ISOLATED WORKTREE at .claude/worktrees/superapp-app-config, branch feat/app-config-provider (off main @ cc4bf43).
Spec: /c/unirefund/docs/superpowers/specs/2026-09-04-application-configuration-provider-design.md (separate docs repo)

## Environment facts established before Task 1
- **A SECOND SESSION IS LIVE IN THIS REPO.** `.claude/worktrees/superapp-nativewind-migration` (branch refactor/superapp-nativewind @ 4f2d689) is registered and **locked**. Never touch it, never `git worktree remove` it, never `git reset --hard`/`stash`/`clean` anywhere here. `npm test` from any worktree picks up sibling worktrees' tests, so a repo-wide test TOTAL is meaningless — compare NAMED SUITES only.
- main checkout is clean at cc4bf43. `.claude/worktrees/` is gitignored (documented in .gitignore line 30-31); `.superpowers/` is already gitignored too.
- Dependency SATISFIED: core/main is at 16f6dc6 = the merged plan-2 PR #22.

## Incoming-merge classification (the durable rule from plan 6's final review)
`git log main..core/main` = **13 commits, 19 files, +1029/-53. ALL THIRTEEN are the app-config feature.**
Zero commits outside the feature — nothing behavioural riding along, no release note needed. This is the clean counterpart to plan 6, where the same check would have caught the auth-cookie rename at task time instead of at final review.

## Pre-flight conflict scan
| Rows checked | Finding |
| --- | --- |
| T1 & T2 share `src/providers/SessionProvider.tsx` | T1's merge brings core's version; T2 strips the country-settings calls from `getUserData` and updates `clearSessionScopedStores`. Strictly sequential. No conflict. |
| T1 & T2 share `src/store/application-configuration.ts` | T1 brings it from core; T2 adds `getCurrencySymbol`, `DEFAULT_CURRENCY_CODE`, `useCountryCode2`. Sequential. No conflict. |
| **T1 & T3 share `src/app/_layout.tsx`** | **FLAGGED.** Plan 2 mounted `ApplicationConfigurationProvider` in core's `_layout.tsx`. The merge will therefore carry that mount into super-app — very likely as a CONFLICT, since super-app's layout has extra providers core does not. So T3 ("Mount the provider") may already be satisfied by T1's conflict resolution. |
| T2 & T3 | No shared files. |
| T4 | Modifies nothing. |
| Each task vs itself | T1 merge-then-typecheck-then-commit is coherent; T2's 9 steps end with type-check+test before commit; T3 reads the site before editing; T4 is gates only. No task specifies a test that contradicts its own code. |
| Plan vs review rubric | No no-op tests, no mandated verbatim duplication. Clean. |

**Ruling 1 (T1/T3 overlap):** T3 does NOT blindly add a mount. Its implementer must first check whether T1's merge already mounted `ApplicationConfigurationProvider`; if so, T3 becomes *verify and correct the placement*, not *add*. Cost if wrong: a duplicated provider, which in this repo is not merely redundant — see Ruling 2.
**Ruling 2 (provider ORDER is load-bearing here, and no test catches it):** super-app's `_layout.tsx` ordering has caused real boot crashes. `BottomSheetModalProvider` must sit ABOVE the toast host, and hooks called inside a `<BottomSheet>` child lose context (`useToast()` throws; `useLocalization()` silently renders raw i18n keys). Router mock-tests do NOT catch either. Whoever places `ApplicationConfigurationProvider` must state WHERE in the tree and WHY, and must not assume a green jest run proves the tree boots.
**Ruling 3 (T4 Step 3, on-device early-refund check):** as of 2026-09-03 the traveller account and the only DEBUGGABLE build sit on DIFFERENT devices, so a traveller-facing JS path has no on-device route. If the early-refund gate is traveller-facing, Step 3 is NOT executable — report it as not-run with the reason rather than faking it or silently skipping. Do not let a blocked device step hold up the rest of Task 4.

## Measured baseline (worktree, 2026-09-08) — MEASURED, not quoted
Setup a fresh worktree needs: `cp ../../../.env .env` (gitignored) then `npm run init`. Without init, BOTH `npm test` and `npm run typecheck` abort in their `pretest`/`pretypecheck` guard because src/data/**/*.gen.json are missing — **the gates do not run at all**, so an unwary baseline reads as "no output = clean". The repo's guard message is explicit about this; trust it. `.env` has SUPPORTED_LOCALES, which init needs.
| Gate | Result |
| --- | --- |
| `npm run typecheck` | **clean, 0 errors** |
| `npm run lint` | **0 errors, 89 warnings** |
| `npm test` | **187 suites: 2 failed / 185 passed. 1779 tests: 4 failed / 1 skipped / 1774 passed** |
BASELINE FAILING SUITES (compare these NAMES, never the counts): `src/screens/traveller/Cards/__tests__/CardScannerModal.router.test.tsx` and `src/components/ui/__tests__/tokens.test.ts`.
**Sibling-worktree pollution did NOT occur**: zero PASS/FAIL lines reference a worktrees path. My memory note about `npm test` picking up siblings applies when running from the MAIN checkout (where the other session's worktree is nested underneath); from inside my own worktree the sibling is not in the tree, so these totals ARE meaningful here. Recorded so the next session does not discard good numbers.
**Stored memory was STALE**: it said only `tokens.test.ts` fails at baseline. There are TWO failing suites now; `CardScannerModal.router.test.tsx` is new and took 89.7s, so it may be a timeout flake rather than a real assertion failure — re-running it isolated to find out. Either way it is a PRE-EXISTING failure on an untouched tree at cc4bf43, not something this plan caused.
**CORRECTION to the line above — my memory was NOT stale, I was wrong.** Re-ran both suites isolated: `CardScannerModal.router.test.tsx` **PASSES** on its own. It is a TIMING FLAKE under full-suite parallel load (89.7s in the full run, fine alone). `tokens.test.ts` fails DETERMINISTICALLY with 3 real assertions — colour tokens: "uses no Tailwind default-scale colour class" (+5), "uses no bare white or black colour class" (+12), "uses no raw hex outside the palette module" (+3). The 4 failed tests in the full run = 3 tokens + 1 flaky CardScanner.
**TRUE BASELINE: exactly ONE deterministic failing suite, `tokens.test.ts` (3 tests)** — which is precisely what the stored memory said. CardScannerModal must be treated as load-dependent noise: if it fails after the merge that is NOT a regression, and it must be re-run isolated before anyone calls it one.

## Task 1
Status DONE_WITH_CONCERNS. **Merge commit 24528fe6 — a REAL two-parent merge** (cc4bf43 + 16f6dc6), 13 commits / 19 files as classified. Rename detection preserved for future syncs.
Gates: typecheck clean 0 errors; lint 0 errors / **89 warnings = exactly baseline**; npm test **192 suites (was 187; +5 from core's incoming tests), 1 failed = tokens.test.ts ONLY**; 1817 tests, 3 failed / 1 skipped. No new deterministic failures. The CardScannerModal flake PASSED this run, confirming it is load-dependent noise.
4 conflicts: `src/app/_layout.tsx`, `src/providers/SessionProvider.tsx`, `jest-setup.ts`, `AGENTS.md`. `src/actions/AccountService/actions.ts` did NOT conflict — super-app had never diverged there, so core's rewrite applied wholesale.
**Provider placement (the thing I flagged pre-flight):** `<ApplicationConfigurationProvider>` sits directly inside `<SessionProvider>`, wrapping `<LocalizationProvider>` — core's exact relative position, super-app's tree otherwise verbatim. Justification given and it is a good one: the provider reads NO React context (only createContext/useMemo/one zustand selector), renders no UI and no sheet, so the `ToastProvider -> BottomSheetModalProvider -> ToastHost` chain is byte-for-byte unchanged; and it sits ABOVE the gorhom portal host so teleported sheet content can still reach it. Honest caveat disclosed: `providerOrder.router.test.tsx` mocks it as a pass-through, so jest proves SHAPE, not BOOT. No device run.
**Ruling 1 discharged: it IS already mounted. Task 3 reduces to VERIFY placement, not add a mount.** Brief Task 3 accordingly.
**Symbol collision to carry into Task 2:** `useEarlyRefundAvailable` now exists TWICE — the pre-existing `src/store/country-settings.ts` one (still the only one consumed, by TagDetailScreen.tsx) and the incoming `src/store/application-configuration.ts` one, backed by a DIFFERENT endpoint and grant. No compile clash. Task 2 Step 3 ("Repoint useEarlyRefundAvailable") is exactly this.
Concern 1 (transient, Task 2 fixes): login now hits country-settings AND application-configuration twice, because `getApplicationConfigurationApi` calls `getCountrySettingsInfo()` internally while super-app's `getUserData` still calls it separately. Expected mid-plan state.
Concern 3 — **GOOD CATCH, a real bug prevented.** Core hung `clearConfiguration()` off an environment effect super-app does not have. The agent moved it into `clearSessionScopedStores` instead; without that, persisted `country`/`settings` would SURVIVE SIGN-OUT into the next account. Same cross-account-leak class as the Novu shared-subscriber bug. Deliberate port beyond the conflict markers, disclosed.
Concern 4 resolved by ME, not the agent: `package-lock.json` was dirty from MY OWN `npm install`, not from the task. Verified the diff was 26 insertions, ALL `"dev": true,` metadata churn, zero dependency changes — reverted with a targeted `git checkout -- package-lock.json` (not a reset/stash/clean). Tree now clean; sibling locked worktree verified untouched.
Task 1: review **APPROVED** — spec ✅ on all 8 rows, 0 Critical, 2 Important, 7 Minor. Reviewer verified the merge is a genuine two-parent commit by reading the raw object (parents cc4bf43 + 16f6dc6), not by trusting the report.
**PROVIDER PLACEMENT: CORRECT, and better-evidenced than the report claimed.** Reviewer checked every premise against source: the provider body is one zustand selector + useCallback + useMemo returning only `<Context.Provider>` — no useToast, no useLocalization, no sheet, and the `use(...)` is in the CONSUMER hook not the provider. Decisively, `git diff -w cc4bf43 HEAD -- src/app/_layout.tsx` is **exactly 3 added lines** (import + open tag + close tag) — not one line inside ToastProvider -> BottomSheetModalProvider -> ToastHost moved. And core's own _layout change at 16f6dc6 is STRUCTURALLY IDENTICAL, so this hunk will not re-conflict on the next sync. Also confirmed the selector picks a zustand action whose identity never changes, so no new app-wide re-render pressure.
**IMPORTANT 1 — the report's justification for its own good catch was FACTUALLY WRONG.** super-app DOES have an environment switch; it lives in `src/store/tenant.ts:72` `setEnvironment`, whose own comment says "Tenants are environment-specific, so a selection cannot survive the switch" and which clears tenantId/publicTenants but NOT the configuration. `country` and `settings` are equally environment-specific and equally persisted. Small blast radius today (EnvironmentChips renders only on StaffLoginScreen and debug-menu, both signed-out, and the window closes on the next getUserData) — but **after Task 2 the consumers are live** (useAppCurrency, useCountryCode2, useEarlyRefundAvailable). The catch was right; the reason given was not, and a future core sync will re-read that reason. -> Task 2.
**IMPORTANT 2 — the one deliberate behaviour change ships with ZERO coverage.** clearConfiguration() is tested at the STORE level, but its WIRING into clearSessionScopedStores is not. `sessionLifecycle.router.test.tsx` already has the exact template (its "clears the tag list on sign-out" / "...when the session expires" pair asserting on store state). Without a sibling test, **Task 2 Step 6 rewrites this very function and can silently drop the line.** -> Task 2.
**MINOR 3 — CORRECTION to what I recorded from Task 1's report (and to what I told the user).** `/api/abp/application-configuration` is fetched **ONCE, not twice**: `getApiAbpApplicationConfiguration` has exactly one call site, reached only via getApplicationConfigurationApi, called once per auth path. The merge actually took that endpoint from 2 -> 1. Only `country-settings/info` is genuinely duplicated. Task 2's implementer must not go hunting for a second config call that does not exist.
**MINOR 4 — the clean typecheck is partly an illusion, and it is the SAME totality bug as the web side.** `SessionProvider.tsx:315` `let configResult;` is an implicit `any`; that is the only reason tsc passes. `UserProfile.grantedPolicies` is `Record<Policies, boolean>` where `Policies = keyof typeof policies.gen.json` = **1094 REQUIRED keys** under strict mode, and `configResult.policies` is `Record<string, boolean>`, which is not assignable. It also means `configResult.user.isAuthenticated` is unchecked at compile time. Exactly the mapped-type-over-a-literal-union overclaim that plan 5 fixed in web-app with `Partial<Policies>`. -> Task 2 (annotate the let AND widen grantedPolicies to `Record<string, boolean>`, which is already what isActionGranted accepts).
**MINOR 8 — PLAN DEFECT.** Task 2 Step 6 says "Replace clearCountrySettings() with the configuration store's clear", but Task 1 ALREADY added clearConfiguration(). Read literally it produces a duplicate call. Patching the plan.
Minor 6 (providerOrder pass-through mock is avoidable by extending the `@/config/env` stub with `env: { gatewayDomain }` — worth ONE attempt in Task 3, it would close the only place a boot failure could surface in jest), Minor 7 (AGENTS.md gate table stale: says 130/131 suites, 1044 tests, 74 warnings vs measured 192/1817/89 — Task 4, and note core re-measured its own table inside this changeset while super-app's was not), Minor 9 (login worst case up to 5s slower on a hanging country-settings, because getApplicationConfigurationApi AWAITS it under Promise.allSettled with a 5s timeout where getUserData previously fire-and-forgot; typical login is FASTER at 2 requests instead of 3; persists past Task 2 by design).
Concerns 1/3/4 all verified sound: SessionProvider resolution is faithful and did NOT do Task 2's work early; the duplicate useEarlyRefundAvailable has no barrel to collide through and exactly one consumer still on the old module; and the three test edits are meaningful — the SessionProvider mock is STRONGER than what it replaced, spreading real EMPTY_APPLICATION_CONFIGURATION via jest.requireActual INSIDE the factory, correctly sidestepping the object-literal hoisting trap.
Task 1: complete (merge 24528fe6, approved, no fix round — both Importants fold into Task 2)

## Task 2
Status DONE_WITH_CONCERNS. Commit 3c28b0d7. Gates: typecheck clean 0 errors; lint 0 errors / **89 warnings = flat**; tests **192 suites, 1 failed (tokens.test.ts only)**; 1819 tests (+2 = Step 6b's), 3 failed / 1 skipped. No new failures.
**CONCERN 1 — Step 7a's premise (which came from the Task 1 REVIEWER's Minor 4, and which I put into the plan) is FACTUALLY WRONG.** Implementer disproved it with four tsc runs plus a minimal probe: (a) `Record<string, boolean>` **IS** assignable to `Record<Policies, boolean>` — a source INDEX SIGNATURE satisfies target literal keys, so there was never an assignability error to fix; (b) the load-bearing implicit `any` was never `configResult`, it was **`let userResult;`**, whose evolving-any spread into `setUser({...userData, …})` made the whole argument `any` — THAT is why grantedPolicies was unchecked; (c) `configResult.user.isAuthenticated` is NOT unchecked once annotated, because TS narrows it (the catch returns). Implementer applied my mandated widening anyway, AND annotated `userResult`, which is the change that actually makes `setUser` type-checked. No cast, no `!`, no @ts-expect-error anywhere.
**COST of the widening I mandated: it REMOVES literal-key typo detection at 7 direct `grantedPolicies[...]` index sites** (4 TagScreen, 2 TagDetailScreen, 1 customs HomeScreen). So my instruction made type safety WORSE for a problem that did not exist. Implementer flagged it as reviewer's call rather than silently keeping or reverting — correct escalation.
My provisional read (putting it to the reviewer): neither the original `Record<Policies, boolean>` (overclaims totality — the payload only ever holds granted keys) nor `Record<string, boolean>` (loses typo detection) is right. The web side already solved exactly this with `Partial<...>`, which keeps literal-key checking AND drops the totality claim — at the cost of widening `isActionGranted` to accept `boolean | undefined` values, which is precisely what web-utils/policies/utils.ts had to do. Asking the reviewer to rule between revert / keep / adopt Partial.
**CONCERN 2 — MY PLAN DEFECT #9, and the largest so far.** The brief claimed 3 consumers outside SessionProvider; there were **11 source modules + 4 test modules**. `useCurrencyCode` (7 consumers) and `useCurrencySymbol` (1) appear NOWHERE in the plan, and Step 7's `git rm` is literally impossible without migrating them. Implementer repointed `useCurrencyCode` -> the existing `useAppCurrency` (byte-equivalent semantics) and carried `useCurrencySymbol` across. It also caught three things the plan never mentioned: a stale AdministrationService mock in onboardingAdoption.router.test.tsx, a now-wrong docblock in utils/tagActions.ts, and the whole `"tenant setting values a session carries"` describe — which it **rewrote against `setting.values` rather than deleting**, preserving the affiliation-switch coverage. That last call is the kind of judgement a deletion-happy implementer would have got wrong.
CONCERN 3: Step 6a needed no `require` — plain top-level import; `application-configuration` reaches only `src/config/**`, so the `tenant -> application-configuration` edge is one-directional. No cycle.
**CONCERN 4: Step 6b verified by MUTATION, not assertion-counting.** Implementer deleted `clearConfiguration()` from clearSessionScopedStores, re-ran, got exactly 2 failed / 23 passed with `Expected: false / Received: true` on `isLoaded`, then restored and re-verified green. Both cases assert `isLoaded === true` BEFORE triggering the path so they cannot pass vacuously. This is exactly the rigor the step was written for.
CONCERN 5: left >80-col lines from Task 1's core merge unformatted, formatting only its own lines — because reformatting that block would conflict on every line at the next `git merge core/main`. Correct instinct; same reasoning that made Task 1 a real merge.
Task 2: review **CHANGES REQUESTED**. Spec ✅ on all 11 steps; 0 Critical, 3 Important, 3 Minor.
**TYPING RULING: (c) `Partial<Record<Policies, boolean>>`.** Reviewer copied the tree to scratch and ran the project's own tsc under each variant: baseline clean, (a) `Record<Policies, boolean>` restored **also clean** — so the brief's predicted error genuinely does not exist — and (c) clean. The only thing ever unassignable was the `| null` from `configResult?.policies ?? null`, which the implementer had already removed at source. **My Step 7a was wrong and both agents were right.**
**(c) is FREE here, which neither I nor the implementer knew.** I told the reviewer (c) would cost a widening of `isActionGranted` like it did on web. It does not: `Partial<Record<Policies, boolean>>` is assignable to `Record<string, boolean> | undefined` as-is, because TS's implicit index signature for a mapped type strips `undefined` from optional properties — and it holds even under exactOptionalPropertyTypes, so it is not a fragile accident. All 7 index sites are `!!user?.grantedPolicies?.["..."]`, and `!!` absorbs `boolean | undefined`. No fixture changes. So (c) is ONE line plus an import in src/store/user.types.ts.
Proven by mutation with a deliberate typo at TagScreen.tsx:92: **(b) as shipped COMPILES CLEAN — the typo reads undefined and the risk column silently disappears for every user.** (a) and (c) both raise TS2551 with the right suggestion. (c) strictly dominates (a): same typo detection, without (a)'s totality lie.
**IMPORTANT 1 — real regression this task introduced.** A failed or slow `getCountrySettingsInfo` (5s timeout race) now OVERWRITES good persisted country data: countryResult.value is undefined -> normalizer yields `currency: "TRY"`, `countryCode2: null` -> setConfiguration replaces `country` wholesale. The OLD store called setCountrySettings only inside `.then()`, so a failure left the persisted value standing. On a successful session with a slow country endpoint: every money label falls back to TRY for a non-TRY tenant, `useStickerTag` silently early-returns `{tag: null}` on the merchant path, and `useCreateTag` posts "TR". Mechanism is Task 1's, but **Task 2 removed the fallback that hid it**, so it is ours.
**IMPORTANT 2 — fail-OPEN on the one flag that must fail closed.** The old provider called `setCountrySettingsValues(null)` in its `.catch()`. Now if getApplicationConfigurationApi rejects, getUserData returns false, setConfiguration is never called, and **fetchNewAccessToken DISCARDS that false** (SessionProvider.tsx:352 awaits it then `return true`). So on an affiliation switch with a failed config read, the previous tenant's `settings` survive — including `CountryManagement.EarlyRefund.EarlyRefundAvailable` — and tag detail keeps offering early refund to a tenant that may not have it. The rewritten test only covers the success path, so nothing pins this any more.
**Ruling on 1+2 (they interact — reviewer named the tension, I am resolving it):** treat the two halves differently. `country` is tenant-stable and a default is WRONG data rather than safe data, so **preserve last-known-good `country`** when the incoming country info is absent. `settings` and `policies` are permission-shaped, where stale is dangerous, so **clear them on a failed read** (fail closed). That satisfies both findings without them fighting.
IMPORTANT 3: `useHomeStatus.router.test.ts:24` replaces the ENTIRE `@/store/application-configuration` module with `{ useAppCurrency }`. Defensible when the target was the narrow country-settings store; not now that the module owns the default store plus useIsHost/useTenantTimeZone/useSettingFlag/useEarlyRefundAvailable. The first consumer added under useHomeStatus/useProfileIdentity fails as "not a function". The diff already contains the better pattern (useStickerTag.router.test.ts seeds the real store).
MINOR 4 (the class the implementer ALSO missed, and worth fixing): the deleted store was PERSISTED. Its `"country-settings-storage"` AsyncStorage blob is now orphaned on every existing install, holding the full 17-block CountrySettingsValuesDto — exactly the data-minimisation concern the new store's own partialize docblock argues. signOut no longer does AsyncStorage.clear(), so nothing removes it. One `AsyncStorage.removeItem` on the hydrate path.
Minors 5-6 (duplicated TRY default across two files; a dead `?.` on SessionProvider.tsx:297 given line 316 proves narrowing) — declining, cosmetic, and 5 is faithful to the brief's verbatim instruction.
Reviewer confirmed independently: store deletion complete with zero importers; useCurrencyCode -> useAppCurrency genuinely byte-equivalent INCLUDING the `||`-not-`??` empty-string coercion; the rewritten describe asserts real behaviour on both cases; Step 6a runs synchronously on the switch path and `doHydrate` uses setState directly so a cold start does NOT wipe config; Step 6b cannot pass vacuously (beforeEach clears, each case seeds then waits for isLoaded===true before triggering); zero casts (4 grep hits all inside prose).
Task 2 FIX ROUND 1: DONE, commit 82ebc815 on top of 3c28b0d7. Gates: typecheck clean; lint 0 errors / **89 warnings flat**; 192 suites / 1 failed; **1825 tests** (1819 -> 1825 = six new), 3 failures still tokens.test.ts only.
(c) applied to src/store/user.types.ts ONLY — restored import, `Partial<Record<Policies, boolean>>`, rewritten docblock. Implementer **verified my assignability claim itself under `--strict --exactOptionalPropertyTypes` before relying on it** (both `Record<string, boolean> | undefined` and `!!partial?.["a"]` compile), so isActionGranted and all fixtures are untouched and the userResult annotation survives. Confirmed the payoff by mutation: `ViewRiskLevel` -> `ViewRiskLevle` now raises TS2551 with the right suggestion. Round-0 concern withdrawn.
**The `country` fix was subtler than my ruling assumed, and the implementer's reasoning is better than mine.** I said "preserve last-known-good country when the incoming info is absent", implying a store-only change. It could not be: once the normalizer synthesises `{currency:"TRY", countryCode2:null}`, that block is **byte-identical to a real answer from a TRY tenant with sparse data**, so the store cannot distinguish absent from default and would have to guess. Solution: a new `NormalizedApplicationConfiguration` carrying `country: ApplicationConfigurationCountry | null`, the normalizer emits `null` on absence, and `setConfiguration` resolves `?? state.configuration.country`. `ApplicationConfiguration` itself is UNCHANGED, so every reader still sees a non-null block. Annotating the return surfaced 8 TS18047 errors, all in tests, all legitimate, none silenced with a cast.
**All six new tests verified by mutation, applied and reversed** — and the mutations were chosen to prove the right thing: (1) country overwritten with the default -> 1 failure, `Expected "GBP" / Received "TRY"`, literally the reported symptom; (2) clearGrants no-op -> 2 failures (store + router); (3) deleting the clearGrants **call site** -> router test fails, store test still passes, which is exactly the point — it pins the WIRING, same pattern as Step 6b; (4) retired key removed -> the new retiredStorage test; (5) normalizer restored to the exact pre-fix expression -> normalizer + AccountService tests. It also disclosed that its first attempt at mutation 5 threw a TypeError and "proved nothing precise", so it discarded it and re-ran against the real pre-fix code rather than counting a crash as a pass.
**Deliberate overreach, flagged for my call: `clearGrants()` also clears `features`, one key beyond what I asked. RULING: KEEP IT.** `features` is the same session/tenant-scoped class as `settings`, arrives on the same payload, and is dropped on the same failure path; clearing one and not the other would be knowingly leaving the identical bug one key over. No live consumer today, so the risk of keeping it is nil and the risk of omitting it is a future fail-open.
Side effect: fix 2a dissolved the TRY duplication I had declined as Minor 5 — the default now lives only in normalizeApplicationConfiguration.ts. DEFAULT_CURRENCY_CODE remains as the brief specified, backing getCurrencySymbol's no-argument path.
Task 2 FIX RE-REVIEW: **all four addressed, no blocking issues, ready for Task 3.** Reviewer re-verified the assignability claim in its OWN scratch probe under --strict --exactOptionalPropertyTypes rather than trusting it — correct, since round 0's equivalent claim was wrong — and confirmed a mistyped key raises TS2551.
Confirmed independently: the `| null` **provably does not leak** — a repo-wide sweep shows the only readers of `configuration.country` are inside the store module (partialize, merge, useAppCurrency, useCountryCode2) plus tests; no component/hook/provider reads it, and `useApplicationConfiguration()` has no consumers at all. First load is correct because the store's initial value is EMPTY_APPLICATION_CONFIGURATION whose frozen `country` block is non-null, so `?? state.configuration.country` never yields null. clearGrants fires ONLY in the Promise.all catch — not on hydrate, not on a successful cold start. The 8 TS18047 fixes preserve their value assertions (GBP/FO/Faroe Islands) rather than being weakened to toBeDefined. No casts (the one `as` is the file's existing jest.MockedFunction idiom).
`features` overreach confirmed genuinely inert — outside src/saas it appears only in the normalizer write, clearGrants, and the new test. Ruling to keep it stands.
Report-wording inaccuracy noted (code is right): the round-1 report's prose reads as if `ApplicationConfiguration` gained the `| null`; the diff puts it only on the new `NormalizedApplicationConfiguration`.
**RESIDUAL A — carry to the FINAL whole-branch review, not another fix round.** `clearGrants` sits OUTSIDE the rehydration guard: it does not set `isLoaded`, so merge's `if (current.isLoaded) return current` does not protect it. If the config read fails before the AsyncStorage rehydration resolves, the persisted `settings` — including EarlyRefundAvailable — are re-applied on top of the cleared map, undoing the fail-closed fix. Window is a network round trip vs a local read and the bootstrap awaits hydrateTenantStore() first, so it is unlikely. **Not spinning a fix round**: the obvious patch (set isLoaded in clearGrants) is semantically wrong — isLoaded means "config loaded", and a failed read has not loaded anything — so this needs a design call (a separate marker the merge guard also checks) that the final review should make with whole-branch context.
**RESIDUAL B — same class, also carry.** `ApplicationConfigurationProvider.tsx:58-64` is the SECOND place getApplicationConfigurationApi can reject and its catch only logs — the identical fail-open one call site over. Inert today: `useApplicationConfigurationRefresh` has zero callers in src. Needs `clearGrants()` whenever `refresh` is wired up.
Informational, no action: partialize includes `settings` so clearGrants also empties the persisted copy (fail-closed direction, refetched next auth); stickiness now survives a tenant/affiliation change within one environment because only setEnvironment calls clearConfiguration, not setTenantId — that is the mandated trade-off of "sticky", just named; and a comment-placement nit in retiredStorage.ts.
Task 2: **complete** (3c28b0d7 + 82ebc815, approved after one fix round)

## Task 3
Status DONE. Commit b02738e — **test-only**, one file (`src/app/__tests__/providerOrder.router.test.tsx`). `_layout.tsx` was read but NOT modified, which is the correct outcome.
Step 2 verification, all three passed: (1) exactly ONE ApplicationConfigurationProvider mount, inside SessionProvider, wrapping LocalizationProvider; (2) `git diff -w 24528fe^ 24528fe -- src/app/_layout.tsx` is exactly 3 lines (import + open + close), nothing else moved; (3) ToastProvider -> BottomSheetModalProvider -> ToastHost nesting unchanged from before the merge. **Ruling 1 fully discharged — no second mount was added.**
**Step 3 SUCCEEDED on the single permitted attempt — the jest blind spot is CLOSED.** The agent traced the real dependency chain (ApplicationConfigurationProvider -> @/actions/AccountService/actions -> @/actions/lib -> @/actions/auth/actions -> @/store/tenant -> @/utils/environment, which reads `env.gatewayDomain` at MODULE-EVAL time via extractApexDomain), extended the `@/config/env` mock with `env: { gatewayDomain: "example.com" }`, and deleted the pass-through mock. All 3 tests still pass INCLUDING both negative controls (the ERT-12 and ERT-10 ordering checks), which were untouched and still throw on their mutated shapes. No assertion weakened or removed.
Why this matters beyond the task: this repo's provider-ordering bugs are BOOT CRASHES, and the layout suite was the only place in jest one could surface — but it mocked the provider away, so it structurally could not. It now mounts the real provider. This is the first automated coverage of that hazard.
Gates identical to baseline: typecheck clean; lint 0 errors / 89 warnings; 192 suites / 1 failed; 1825 tests / 3 failed / 1 skipped — all 3 failures confined to tokens.test.ts. CardScannerModal passed this run (flake, as established).
Task 3: review **APPROVED** — spec ✅ on all 5 rows, 0 Critical, 0 Important, 1 trivial Minor (declined: the mock comment does not justify why "example.com" specifically is safe; environment.ts's own docblock already uses example.com as its example domain).
**Negative controls verified INTACT by diffing the file against its pre-change version**, not by trusting the report: the only hunks are the extended `@/config/env` mock and the deleted pass-through, both inside the mock-declaration block (lines 98-134). ZERO context lines touch the describe/it bodies (143-176). Test 2 (ERT-12: BottomSheetModal throws 'BottomSheetModalInternalContext' cannot be null! with no provider ancestor) and Test 3 (ERT-10: useToastRef must be used within ToastProvider) render their mutated shapes directly, independent of Layout, and are byte-for-byte unmodified. No assertion regex, toThrow expectation, or render target altered anywhere. Test 1 is the only substantive change — it now exercises the GENUINE provider instead of a no-op.
Reviewer also swept repo-wide: only three references to ApplicationConfigurationProvider exist (the component, its ONE mount in _layout.tsx, and this test file) — no lingering mock elsewhere and **no `__mocks__` shadow directory**, which is the way this kind of fix usually gets silently undone.
Task 3: complete (commit b02738e, approved, no fix round)

## Task 4 (verification + AGENTS.md)
Status DONE. Commit 1d28156. Gates MEASURED (npm test run TWICE for stability, identical both runs): init clean; typecheck 0 errors; lint 0 errors / **89 warnings**; **192 suites / 1 failed; 1825 tests / 1821 pass / 3 failed / 1 skipped** — only tokens.test.ts. CardScannerModal passed both runs (the flake did not manifest this session).
Step 1 call reduction GENUINELY demonstrated: exactly one production call to getApiAbpApplicationConfiguration (inside getApplicationConfigurationApi) and exactly one to the real CountrySettingsInfo endpoint (inside getCountrySettingsInfo). **Zero live CountrySettingsValues calls** — the only textual hit is an incidental comment in retiredStorage.ts. That is the plan's headline claim (4 session-bootstrap calls -> 2, one fewer grant dependency) actually proven rather than asserted.
**Step 3 (on-device): NOT RUN, honestly reported — exactly the outcome I asked for over a fabricated pass.** Reason: only one device attached, `OrderPAD_3`. It launched to the signed-out role-selection screen with a stale "Refresh token error" banner, and this session holds no traveller or staff credentials for any tenant, so tag detail and the early-refund gate are unreachable regardless of build. No Metro, no deep link, no sign-in attempted; nothing faked.
**NEW DEVICE INFORMATION that contradicts stored memory:** the OrderPAD_3's installed `com.clomerce.unirefundsuperapp` build **IS DEBUGGABLE**. My memory said the debuggable build was the V3 only, NOT the OrderPAD. Updating that memory.
Step 4 (prettier): only `src/store/application-configuration.ts` had a real content change — a pure re-wrap, confirmed whitespace-only by re-running typecheck. **The other four listed files showed as `M` in git status but were BYTE-IDENTICAL to HEAD** — a `core.autocrlf=true` + mtime artifact. The agent proved it three ways (git hash-object, cmp, and staging each individually to compare the staged blob hash against HEAD) and committed only the two genuinely-changed files. That is the right instinct: committing phantom CRLF churn would have polluted the diff with four no-op files.
Final `git diff --stat cc4bf43..1d28156`: **42 files, +1475 / -301** — the whole four-task change.
Task 4: complete (commit 1d28156; no task review per the standing ruling — its only file change is documentation and the deliverable is measurements)

## FINAL WHOLE-BRANCH REVIEW (opus, cc4bf43..1d28156, two passes)
Verdict **Ready to merge: WITH FIXES.** 0 Critical, 4 Important, ~10 Minor.
VERIFIED AGAINST SOURCE, not reports: merge is genuinely two-parent and `git merge-base HEAD core/main == core/main` so nothing of core's is unmerged; country-settings.ts shows as a clean `delete mode`, so rename detection survived. **Headline claim TRUE, checked at the BASE commit**: at cc4bf43 `getGrantedPoliciesApi` AND `getCurrentUserIdApi` each issued a SEPARATE FULL application-configuration round trip for one field each, plus the two country-settings calls = 4. Head has exactly one of each = 2. `getCountrySettingsValues` and its `UniRefund.Settings.GetValues` dependency are gone with zero references. (Caveat: `getCountrySettingsInfo` still needs its own `GetInfo` grant; only the *values* grant dropped.)
Reviewer strengthened Ruling 2 beyond what I recorded: the provider imports `@/actions/AccountService/actions`, which SessionProvider ALREADY imported — so it adds **no new module-eval graph at boot**, which is the actual mechanism behind this repo's boot crashes. It also confirmed every migrated consumer is a zustand SELECTOR not a context read — including CartReviewSheet.tsx:32 and AmountDisplay.tsx:41, which live INSIDE sheets — so the portal context-loss trap is untouched.
**IMPORTANT 1 — a second fail-open path, and it IS a regression against the base.** `SessionProvider.tsx:318-325`: the validation early-return (`!userData || !userId || !jwtUser || !configResult.user.isAuthenticated`) returns false WITHOUT clearGrants(); only the catch at 286-300 got it. Reachable on an affiliation switch where the config read SUCCEEDS with tenant B's payload but the profile read resolves falsy / decodeJWT fails / ABP reports isAuthenticated:false (that endpoint answers 200 for anonymous, so the 401-retry never fires). getUserData returns false, fetchNewAccessToken:359 discards it, session continues — store still holds **tenant A's EarlyRefundAvailable**, with fresh authoritative data thrown away. **At cc4bf43 this could not happen**: the fire-and-forget `setCountrySettingsValues(v ?? null)` sat ABOVE the gate, so every auth path refreshed or nulled the flag. Core's 70d64ee moved the commit below the guard and removed that.
**IMPORTANT 2 — the new comments assert a property the code does not deliver.** `configuration.policies` has **ZERO production readers**; every policy gate reads `useUserStore().user.grantedPolicies` (13 sites listed). So clearGrants() clears the copy nobody reads and leaves the live one holding the previous tenant's grants. The SPECIFIC bug Task 2 named (EarlyRefundAvailable) IS genuinely fixed, because `settings` does have a reader — but the generalisation to "grants" is false. Pre-existing hole, yet the new docblocks now claim otherwise, which is worse than silence.
**IMPORTANT 3 — MY CONTROLLER MISTAKE.** Commit 1d28156 reformats 226 lines of application-configuration.ts under a `docs:` subject. Whitespace-only, but `create<ApplicationConfigurationStore>()(` is 81 chars in core so prettier split it and re-indented the whole persist(...) body — regions super-app never semantically changed (partialize, merge, useIsHost, useTenantTimeZone, useAppCurrency) are now line-for-line different from core and will conflict on any core edit. **Task 4 Step 4's blanket `prettier --write` directly contradicted Task 2's Concern 5, which I had already accepted as correct. I should have amended that plan step when I accepted Concern 5 and did not.**
**IMPORTANT 4 — matters for PLAN 4.** Both behaviour fixes live only in super-app, inside CORE-OWNED files: core still ships the fail-open normalizer and has no clearGrants at all. So pos-app and every future derived app inherit both bugs — while the AGENTS.md this very branch writes says to "Change them in core and merge them down". Needs a core-mobile follow-up porting NormalizedApplicationConfiguration, the `country ?? state.configuration.country` resolution, and clearGrants upstream. **Do this BEFORE plan 4 starts**, since plan 4 mirrors core by design.
**RESIDUAL A: reviewer went FURTHER than my ruling — the window is UNREACHABLE, not merely unlikely**, and gave the structural proof: clearGrants fires only in getUserData's catch, whose two callers are adoptSession (on false it calls publishAccessToken(null) and never reached setUser; useUserStore is NOT persisted, so at cold start isSignedIn is false and tag detail is unreachable — the suite already asserts landing on `Landing`) and fetchNewAccessToken (requires a live signed-in session, many frames after rehydration, and the bootstrap awaits hydrateTenantStore's AsyncStorage reads before any network call). So the window in which merge can undo the clear is exactly the window in which nothing consumes the result. **Confirms: do NOT set isLoaded** — it would additionally make merge return `current` and discard the persisted `country` that clearGrants deliberately preserves. Action: document the window AND the two invariants that keep it inert, in the docblock, not the ledger.
**RESIDUAL B: ship, but the comment must be corrected — it argues the WRONG way.** ApplicationConfigurationProvider.tsx:53-57 currently says "A failed load leaves whatever the store holds — on a cold start that is the fail-closed empty configuration". That is exactly the reasoning Task 2 disproved; the hook exists for tenant/affiliation switches, the non-cold-start case where "whatever the store holds" is the previous tenant's data.
**Reviewer CORRECTED my ledger, and is right:** my note that fix 2a "dissolved the TRY duplication" is FALSE — both `DEFAULT_CURRENCY_CODE` (application-configuration.ts:160) and `DEFAULT_CURRENCY` (normalizeApplicationConfiguration.ts:10) still exist. Also, the plan's premise that DEFAULT_CURRENCY_CODE/getCurrencySymbol "have live consumers" was wrong at cc4bf43 too — they have none outside their own module.

## FINAL FIX WAVE
Done. Two commits on 1d28156: **f0bd2b9** `revert: undo the whitespace reformat of a core-owned file` (item 3, deliberately kept SEPARATE so the restore is not tangled with semantics) and **527fd74** `fix(app-config): fail the second auth path closed, and clear the map gates read` (items 1,2,4,5,6). Gates: typecheck clean; lint 0 errors / **89 warnings exactly**; 192 suites / 1 failed; **1827 tests** (1825 + 2 new) / 3 failed / 1 skipped. tokens.test.ts still the only failing suite; CardScannerModal passed in the full run.
**Item 1 mutation-verified, and the test guards BOTH directions.** Deleting the `revokeSessionAuthority()` call from the early-return failed exactly one test — the new one — with `+ "CountryManagement.EarlyRefund.EarlyRefundAvailable": "true"`, i.e. the previous tenant's flag surviving, which IS the regression. Restored -> 28/28. The same assertion also pins core's ordering in the opposite direction: hoisting setConfiguration above the gate would yield the new tenant's "false" instead of `{}` and still fail. Item 2's case mutation-tested the same way.
**Item 2: no ripple** — exactly one interface line plus one action in src/store/user.ts, no call site outside SessionProvider changed, no type changed, no existing test touched. **It also corrected my brief's premise:** `isSignedIn` is `!!user?.userId || !!session` (RootNavigator.tsx:25), not `!!user` as I wrote. Conclusion unchanged and firmer; it put the real expression in the docblock rather than propagating my paraphrase.
Note: the agent wrapped the two clears in a local helper `revokeSessionAuthority()` rather than calling both inline. Not in my brief — a design choice for the re-review to judge.
**Item 3 verified by the right check.** `git diff -w core/main HEAD -- src/store/application-configuration.ts` is now PURELY SEMANTIC: partialize, merge, useIsHost, useTenantTimeZone and useAppCurrency's code line no longer appear at all; the one-line `create<...>()(` and core's persist(...) indentation are restored. Seven hunks remain, all legitimate.
**Ruling on the two residual comment hunks (useAppCurrency / useEarlyRefundAvailable docblocks): KEEP.** The agent traced them with `git log -L` to 3c28b0d — a SEMANTIC commit, not the prettier run — and the code lines beneath them are byte-identical to core. They are documentation of super-app-specific behaviour, not formatting churn, and they will travel upstream with the item-4 core port. Flagging rather than silently deleting them was the right call.
FOLLOW-UP (recorded, not fixed): `fetchNewAccessToken` still DISCARDS getUserData's `false` — every fix in this wave mitigates that rather than removing it. That is the underlying design issue behind both fail-open findings and deserves its own ticket.
FOR THE UPSTREAM CORE PORT: `revokeSessionAuthority()` is super-app-local and **core has no `clearGrantedPolicies` to call yet**, so the port needs the user-store action too, not just the normalizer and clearGrants.
AGENTS.md test count bumped 1825 -> 1827 so the table is not wrong on landing.
FINAL-FIX RE-REVIEW: **all six addressed, READY TO MERGE**, no functional issues. Two nits only (a one-character markdown table misalignment in AGENTS.md; the item-1 test does not also pin `country`, so a hypothetical "hoist setConfiguration AND keep the revoke" edit would pass — narrow, not worth a round).
Both claims I asked to be checked independently HELD: (1) the new test is genuinely non-vacuous — `clearGrants()` is the SOLE writer of `settings -> {}` on that path, since `clearConfiguration()` is only reachable via clearSessionScopedStores which runs on explicit sign-out or an onSessionExpired broadcast, neither of which fires there; and `findByText("Landing")` proves getUserData actually returned false. (2) `isSignedIn` really is `!!user?.userId || !!session` (RootNavigator.tsx:25) — my brief's `!!user` was wrong, the implementer's correction right — and clearGrantedPolicies spreads state.user replacing only grantedPolicies, so userId survives and no one is signed out.
**Item 3 proven more strongly than reported:** `git diff core/main HEAD` and `git diff -w core/main HEAD` on application-configuration.ts are **BYTE-IDENTICAL** (both 113+/3-, 3 hunks) — that proves ZERO whitespace-only difference from core remains, a stricter result than "-w looks clean". And `git diff --stat b02738e f0bd2b9` is EMPTY, so f0bd2b9 is a pure restore. Both surviving docblock hunks trace via `git log -L` to 3c28b0d, the semantic commit, and neither exists in core/main — my ruling to keep them stands.
**`revokeSessionAuthority()` helper: EARNED, keep.** Two call sites that must never drift plus a non-obvious reason they must stay paired (two stores holding different halves of one property). Verified called on BOTH getUserData `return false` sites (the only two) and correctly ABSENT from signOut and the expiry teardown (which go through clearSessionScopedStores, a strict superset), from fetchNewAccessToken's own catch (refreshSession already owns teardown, no tenant switch occurred), and from ApplicationConfigurationProvider.refresh (core-owned, zero callers, documented instead).
Also confirmed: item 2's premise re-verified from scratch (zero production readers of configuration.policies; tagActions' ctx.policies is fed from user?.grantedPolicies); the two new module-level test `let`s are reset in the top-level beforeEach so no cross-test leakage; the second new test is non-vacuous too; and **nothing from the "not doing" list was actioned** — all five declined items verified still standing.
**PLAN 3 COMPLETE** — 4 tasks, 1 task fix round, 1 final fix wave, all reviewed clean.
