# SDD ledger — plan: /c/unirefund/docs/superpowers/plans/2026-09-04-app-config-provider-1-web-app.md

Spec: /c/unirefund/docs/superpowers/specs/2026-09-04-application-configuration-provider-design.md (read, reachable)

## Workspace

- Worktree: `C:/unirefund/.worktrees/web-app-app-config` on branch `feat/app-config-provider`, from `origin/main` @ 870b40710.
- User's checkout `C:/unirefund/web-app` remains on `fix/traveler-document`, untouched and clean. Consent for the worktree given by the user.
- Worktree placed OUTSIDE the web-app checkout because neither `.worktrees` nor `.claude` is git-ignored there; this avoided committing a .gitignore change onto the user's branch.

## Baseline (measured 2026-09-07 in the worktree, post-install + post-init)

| Gate | Command | Baseline |
| --- | --- | --- |
| Unit tests | `cd apps/web && pnpm run test:unit` | 19 suites / 104 tests, 103 pass, 1 skipped, **0 fail** |
| Type-check | `cd apps/web && pnpm run type-check` | **exactly 2 errors**, both `TS2307: Cannot find module '@/public/unirefund.png'` |
| Lint | `pnpm run lint` (repo root) | not yet measured — measure before Task 7 |

Setup notes: submodules required `git submodule update --init --recursive`; `.env` files are gitignored and were copied from the user's checkout into the worktree; `pnpm run init` (in `apps/web`) must run before type-check or 7 phantom codegen errors appear instead of 2.

## Pre-flight conflict scan

### Cross-task pairs (shared file or interface)

| Pair | Produces → Consumes | Finding |
| --- | --- | --- |
| T1 → T2 | T1 creates `app-config/logic.ts` (3 re-exports); T2 extends it to 5 | Consistent; T2 edits the file T1 created. OK |
| T1 → T2 | T1 `types.ts` → T2 `normalize.ts` imports `ApplicationConfiguration`, `CountryInfo`, `RawApplicationConfiguration` | OK |
| T2 → T3 | T2 `EMPTY_APPLICATION_CONFIGURATION`, `normalizeApplicationConfiguration` → T3 `fetch.ts` imports both from `./logic` | OK — both re-exported by T2's logic.ts |
| T3 → T6 | T3 `getApplicationConfiguration()` → T6 calls it in both providers.tsx | OK |
| T4 → T5 | T4 `useApplicationConfiguration` via `@repo/utils/app-config` (index.tsx) → T5 tenant.tsx + granted-policies.tsx import it | OK, and T4 precedes T5 — correct order |
| T4 → T6 | T4 `ApplicationConfigurationProvider` → T6 mounts it | OK |
| T2 → T5 | T2's contract fields (`country.countryCode2`, `country.currency`, `timeZone`, `tenant.*`) → T5's `useTenant` adapter reads exactly these | OK — every field T5 reads exists in T2's contract |
| T2 → all | T2 moves `isHostTenant` to the submodule and leaves a re-export at `apps/web/src/utils/is-host-tenant.ts` | OK — existing importers (`require-host.ts`, sidebar nav) keep resolving |
| T5 → T6 | T5 makes `tenantData` an optional ignored prop; T6 drops it from the call site | OK — ordering means T5's optionality lands first |
| T1/T2 → T7 | T7 asserts `git diff main...HEAD -- apps/web/src/app` is empty | OK — all `apps/web` edits are under `src/providers`, `src/utils`; none under `src/app` |

### Task-internal consistency

| Task | Finding |
| --- | --- |
| T1 | Tests import `@repo/utils/app-config/logic`, added as an export in the same task. Self-consistent. |
| T2 | Fixture cast `as unknown as RawApplicationConfiguration`; `normalizeApplicationConfiguration({}, undefined)` is valid since all DTO fields are optional. OK |
| T3 | `fetch.ts` deliberately omits `"use server"` so it is not published as an RPC endpoint; imports a `"use server"` module, which is legal server-side. OK |
| T4 | `index.tsx` filename is required by the `./*` → `./*/index.tsx` export map. OK |
| T5 | Adapter returns every field the 122 `useTenant` consumers destructure. OK |
| T6 | Provider nesting places ApplicationConfigurationProvider above both adapters. OK |
| T7 | Verification only. OK |

## Rulings

- **Ruling 1 (blocking, structural): `packages/utils` is a git submodule, not a directory.** It is the separate repo `web-utils`, checked out DETACHED at `cd44477` (== its `origin/main`). Tasks 1–5 write most of their code there, and their commit commands (`git add packages/utils/...` from the superproject) fail with `fatal: Pathspec ... is in submodule`. Decision: before Task 1, create branch `feat/app-config-provider` inside the submodule; every task that touches `packages/utils/**` commits INSIDE the submodule first, then the superproject records the pointer bump as a second commit in the same task. Delivery becomes two PRs (web-utils, then the web-app pointer bump). — Cost if wrong: commit topology and PR count only; no code content changes. If the team vendors this package differently, the commits re-home without touching the source.
- **Ruling 2: the plan's `cd web-app && pnpm run type-check` does not exist.** The root has no `type-check` script (`build`, `dev`, `lint`, `unit-test`, `format`, …). Decision: the type-check gate is `cd apps/web && pnpm run type-check`. — Cost if wrong: none; a wrong command simply fails loudly.
- **Ruling 3: the plan misattributes the type-check baseline.** It says the 2 pre-existing `TS2307`s are `mrz` errors caused by a missing GitHub Packages token. Measured, they are both `Cannot find module '@/public/unirefund.png'`. The count of 2 is correct; the cause is not. Decision: implementers compare against "exactly 2, both unirefund.png". — Cost if wrong: an implementer could mistake a genuinely new mrz error for baseline. Naming the exact messages removes that.
- **Ruling 4: worktree needed setup the plan does not mention** — submodule init, `.env` copied from the user's checkout (gitignored, so absent from a fresh worktree), and `pnpm run init` before type-check. Without init, type-check reports 9 errors, not 2. — Cost if wrong: none; recorded so a later session reproduces the baseline.

## Progress

- **Ruling 5: review packages must be augmented with the submodule diff.** `scripts/review-package` diffs the superproject only, so for any task touching `packages/utils` it emits the test files plus a two-line `Subproject commit` pointer bump — the implementation itself is invisible to the reviewer. Verified on Task 1: the raw package was 150 lines and contained zero implementation. Decision: after generating each package, run `augment-package.sh <pkg> <sub-base> <sub-head>` (in this workspace) to append the submodule content diff under a labelled header, and tell the reviewer that submodule paths are relative to `packages/utils/`. — Cost if wrong: none; it only adds context. Skipping it would mean every review of Tasks 1–5 rubber-stamped code it never saw.

Task 1: implementer DONE — submodule `0b1ddaa`, superproject `c6bb4fb3d` (includes pointer bump). Tests 119 pass / 1 pre-existing skip / 0 fail; type-check still exactly 2 baseline errors. Implementer noted the pre-commit prettier hook trimmed trailing commas in the two test files.
Task 1: review package `review-870b40710..c6bb4fb3d.diff` (augmented to 350 lines, 8 files visible). Reviewer dispatched.
Task 1: review clean — Spec ✅, quality Approved, 0 Critical, 0 Important.
Task 1: ⚠️ resolved by controller — reviewer could not verify IANA time-zone extraction from this diff; correct, it is Task 2's normalizer, and Task 2's brief carries two dedicated tests for it ("takes the IANA time zone, not the Windows id" and the Intl-accepts assertion). Not a gap.
Task 1: minor (deferred): getSetting/getFeature have byte-identical bodies (plan-mandated, distinct domains).
Task 1: minor (deferred): getFeature/getBooleanFeature exported but untested in this task (matches brief; confirm a later task exercises them before a live consumer exists).
Task 1: minor (deferred): getNumberSetting — Number("") === 0, so an empty-string value returns 0 rather than the fallback. Unreachable today (no numeric key in SETTING_KEYS). REAL latent bug — surface to final review.
Task 1: minor (deferred): getBooleanSetting/getNumberSetting re-implement getSetting's null-coalescing branch instead of delegating.
Task 1: complete (commits 870b40710..c6bb4fb3d, review clean)
Task 2: implementer DONE — submodule `01e3b3d`, superproject `8f3e70f93`. Tests 132 total / 131 pass / 1 pre-existing skip / 0 fail (+12 new). apps/web type-check still exactly 2 baseline errors; packages/utils type-check clean. All 4 pre-existing isHostTenant importers confirmed resolving via the re-export.
Task 2: review package `review-c6bb4fb3d..8f3e70f93.diff` (augmented to 398 lines, 7 files visible). Reviewer dispatched.
Task 2: review clean — Spec ✅, quality Approved, 0 Critical, 0 Important. Reviewer independently confirmed the Windows-vs-IANA test genuinely discriminates (not a tautology) and that the whitelist-style return object makes the field-dropping requirement a structural guarantee rather than a regressable convention.
Task 2: ⚠️ open at controller — reviewer could not tell from the diff whether lint regressed. Measuring the repo lint baseline now, on the post-Task-2 commit, before Task 3 edits anything.
Task 2: minor (deferred): normalize.ts tenantId merge `raw.currentTenant?.id ?? countryInfo?.tenantId` — an explicit `currentTenant.id: null` would be overridden by a non-empty countryInfo.tenantId if the two endpoints ever disagreed. Untested edge; consider an "either says host → host" merge if it becomes observable.
Task 2: minor (deferred): fixture omits the top-level `localization` block, so only `objectExtensions` has an explicit is-dropped assertion; localization/globalFeatures/multiTenancy/clock rely on the structural guarantee alone.
Task 2: minor (deferred): "maps the current user" test asserts only isAuthenticated/userName/roles, leaving id/surName/email/emailVerified/phoneNumber/sessionId unasserted.
Task 2: complete (commits c6bb4fb3d..8f3e70f93, review clean)
Lint baseline (measured post-Task-2, repo root `pnpm run lint`): **exit 0, 0 errors**, 948 warnings — web 465, ssr 459, @repo/ayasofyazilim-ui 14, @repo/ui 10. Task 2's ⚠️ RESOLVED: no lint regression.
NOTE for final review: root lint runs 4 tasks (web, ssr, ayasofyazilim-ui, ui) — `packages/utils` is NOT among them, so all the new app-config code in the submodule has ZERO lint coverage in this repo. The submodule may lint in its own CI; unverified here.
Task 3: implementer DONE_WITH_CONCERNS — submodule `b3f1cc2`, superproject `deb441dd5`. All four gates at baseline: apps/web type-check 2 known errors, packages/utils type-check clean, test:unit 132/131/1/0 unchanged (task adds no tests by design), root lint exit 0 / 0 errors / warnings unchanged.
Task 3: implementer's concern — the brief's PROSE says getGrantedPoliciesApi keeps return type `Policies | undefined`, but the brief's own VERBATIM CODE drops the undefined branch (fail-closed default is `{}`), so the inferred type narrows to `Promise<Policies>`. Implementer followed the code. Passed to the reviewer to judge independently rather than pre-adjudicated.
Task 3: review package `review-8f3e70f93..deb441dd5.diff` (augmented to 218 lines, 5 files). Reviewer dispatched.
NOTE: review-package names files by an abbreviated-but-not-7-char sha (`deb441dd5`), so never guess the package path — capture it from the script's stdout. A guessed path silently creates a NEW file containing only the submodule half, which would have sent the reviewer a diff missing the superproject side.
Task 3: review ❌ Needs fixes — 0 Critical, 2 Important (both plan-mandated), 1 Minor (non-defect).
Task 3: **Ruling: UPHOLD reviewer finding #2 (fail-closed gap).** The spec's Failure-behaviour section requires fail-closed "identical to today's `catch → return undefined`", and the old `getGrantedPoliciesApi` wrapped its ENTIRE body — `auth()`, client construction and the call. The new `getApplicationConfiguration` wraps only the two network calls in `Promise.allSettled`, so a throwing `auth()` (corrupt/expired session-cookie decryption, key rotation) makes the `cache()`-memoized promise REJECT and propagate to every consumer in the request instead of denying access. Spec is the binding authority and the finding is correct. Fix: wrap the body in try/catch returning `EMPTY_APPLICATION_CONFIGURATION`. — Cost if wrong: a 3-line guard that can only convert a crash into a denial; no behaviour is lost.
Task 3: **Ruling: UPHOLD finding #1 as a PLAN defect, keep the code.** The narrowing of `getGrantedPoliciesApi` to `Promise<Policies>` is a strengthening, not a break: reviewer independently walked all ~10 call sites (none does a strict undefined/truthiness check on the object; all use optional chaining or `isActionGranted`) and confirmed `policies.json` is all-`false`, so `{}` is behaviourally identical to the old undefined-triggers-default path. Decision: keep the code, correct the plan's Global Constraints wording which wrongly promises `Policies | undefined`. — Cost if wrong: if a future caller must distinguish "fetch failed" from "legitimately empty", it would need an explicit signal; nothing does today.
Task 3: **Ruling: no unit test for the try/catch.** `fetch.ts` lives in the `packages/utils` submodule, which has ZERO test infrastructure — no test script, no test files, and no gate runs one (root lint covers 4 packages and packages/utils is not among them). The repo's only unit gate is `apps/web`'s `node --import tsx --test "src/**/*.test.ts"`, which cannot reach submodule internals, and all 19 existing suites are pure-function tests with no module-mocking precedent. Requiring a test here would mean building a mocking harness this repo has never had. Decision: implement the guard, no test, justification recorded. — Cost if wrong: the guard is unverified by automated test; it is 3 lines and reviewed by eye twice.
Task 3: fix round 1/5 — implementer wrapped the whole cached callback body in try/catch returning EMPTY_APPLICATION_CONFIGURATION, leaving the inner Promise.allSettled untouched. Commits submodule `9bdce47`, superproject `55fc99ff8`. All four gates at baseline. Scoped re-review dispatched (1 finding sent, 1 ruled a plan defect and fixed in the plan instead).
Plan corrected in docs repo (commit `d864af0`): Task 3's Interfaces block no longer promises `Policies | undefined`, and its fetch.ts code block now carries the outer try/catch with the reasoning. Plans 2-4 (core/super-app/pos-app) do NOT need the same fix — their RN providers already catch around the action.
Task 3: fix round 1/5 (1 addressed, 0 open; commits deb441dd5..55fc99ff8) — re-reviewer confirmed the inner Promise.allSettled independence and the !session fast path both survived byte-for-byte, and additionally verified auth()/getAccountServiceClient/getAdministrationServiceClient contain no redirect()/notFound() calls, so the blanket catch cannot swallow a Next.js control-flow throw. That was the real risk of this fix and it is clear.
Task 3: complete (commits 8f3e70f93..55fc99ff8, review clean after 1 fix round)
Task 4: implementer DONE — submodule `e244474`, superproject `f84af707a`. All four gates at baseline. Implementer empirically verified the `@repo/utils/app-config` barrel resolves (temporary import, deleted pre-commit); controller confirmed both worktree and submodule `git status` are clean, so nothing stray survived.
Task 4: review package `review-55fc99ff8..f84af707a.diff` (augmented to 82 lines, 3 files). Reviewer dispatched.
Task 4: review clean — Spec ✅, quality Approved, ZERO findings at any severity. Reviewer independently corroborated export-map resolution (`"./*": "./*/index.tsx"` with no competing exact key for the bare subpath) plus apps/web's `moduleResolution: "bundler"`, which honours exports maps.
Task 4: complete (commits 55fc99ff8..f84af707a, review clean)
Task 5: implementer DONE — submodule `ede810e`, superproject `0942e4d2a`. All four gates at baseline. Controller independently verified `git diff --stat 870b40710..HEAD -- "apps/web/src/app"` is EMPTY across the whole branch: zero consumer files touched, which is the migration's non-breaking proof.
Task 5: implementer concern passed to reviewer unjudged — outside a provider the empty-config fallback now yields currency "USD" (was ""), timeZone "UTC" (was ""), grantedPolicies {} (was the all-false policies.json). Reviewed on opus given 267 consumers depend on this contract.
Task 5: review package `review-f84af707a..0942e4d2a.diff` (augmented to 248 lines, 3 files). Reviewer dispatched.
NOTE for final review: neither tenant.tsx nor granted-policies.tsx is reachable by ANY automated test in this repo — the only unit gate is apps/web's `node --import tsx --test "src/**/*.test.ts"`, which is .ts-only and cannot execute JSX. Type-check is the sole automated evidence for the riskiest task in the plan.
Task 5: review ❌ Needs fixes — 0 Critical, 2 Important, 5 Minor.
Task 5: **Ruling: UPHOLD Important #1 (empty-string timeZone crashes Intl), fix in normalize.ts not the adapter.** Old code coerced `props.tenantData.timeZone || "UTC"` and `currency || "USD"`; the adapter now reads config values raw and normalize.ts uses `??`, which only short-circuits null/undefined — an empty string from `countryInfo.timeZone` survives into `Intl.DateTimeFormat`, which throws `RangeError: Invalid time zone specified:` at render across ~120 `formatToTenantDate` consumers. The old guard sat on exactly the field normalize.ts now reads, which is evidence "" was considered reachable. Decision: change the `??` chains for timeZone and currency in normalize.ts to `||`, so ONE fix covers every consumer (and the RN copies in plans 2/4), plus a pure .ts test — which the existing apps/web gate CAN run, unlike anything touching tenant.tsx. — Cost if wrong: `||` also coerces a legitimate falsy setting; neither field has a meaningful falsy value, so there is none.
Task 5: **Ruling: Important #2 is NOT a Task 5 code defect.** apps/ssr consumes useGrantedPolicies (tag-claim.tsx, upload-verification-dialog.tsx) and mounts the now-inert `grantedPolicies` prop; apps/ssr has no ApplicationConfigurationProvider until Task 6, whose brief already covers it. This is the expected intermediate state of a 7-task plan, not a regression to fix here. BUT the reviewer is right that there is no compile-time tripwire — `grantedPolicies?: Policies | undefined` and `tenantData?: unknown` both accept-and-ignore, so a forgotten mount type-checks clean, lints clean, passes the unit gate and fails closed at runtime with every permission-gated control vanishing. Decision: hard-gate Task 6 on BOTH apps/web and apps/ssr providers.tsx, and do not accept a green type-check as evidence either app is wired — require the mount to be shown in the diff for each. Carried into Task 6's dispatch and its review. — Cost if wrong: none; it only adds a check.
Task 5: minor (deferred → folded into fix round, all in files already being touched): dead `policies.json` import in granted-policies.tsx (no gate catches it — packages/utils has no lint script and noUnusedLocals is unset); missing @deprecated markers on the two ignored props (cheapest available substitute for the missing tripwire in #2); stale `TenantContext` reference in is-host-tenant.ts docblock, rot caused by this diff.
Task 5: minor (deferred, NOT fixed): provider.tsx context value is unmemoized, so an inline `configuration={{...}}` at a future call site would silently revert all 122 consumers to a fresh object per render — check during Task 6 review.
Task 5: fix round 1/5 — normalize.ts `??` → `||` for timeZone and currency, +2 pure normalize tests covering the empty-string path, dead policies.json import removed, @deprecated markers on both ignored props, is-host-tenant docblock de-rotted. Commits submodule `f5d2906`, superproject `0f8449c05`. Tests 134/133 pass/1 skip/0 fail; other three gates at baseline; apps/web/src/app still empty in the diff. Scoped re-review dispatched.
Plans corrected in docs repo (commit `2a9046d`): the `??` → `||` normalizer fix applied to plan 1 AND plan 2 (core). Plan 4 (pos-app) copies core's normalizer file verbatim so it inherits the fix; plan 3 (super-app) merges core. Without this the same crash would have shipped into all three RN apps.
Task 5: fix round 1/5 (4 addressed, 0 open; commits 0942e4d2a..0f8449c05) — re-reviewer hand-traced BOTH new tests against the old `??` code and confirmed each would have FAILED pre-fix (`undefined ?? "" ?? "UTC"` → `""`), proving they cover the real regression rather than passing incidentally. Also confirmed `||` is safe: both fields are typed plain `string`, so no legitimate falsy value can be swallowed, and the other fields (countryCode2/3, countryName, tenant.id/name) correctly still use `??`.
Task 5: complete (commits f84af707a..0f8449c05, review clean after 1 fix round)
GAP IN MY OWN BASELINE, now closed: I had only measured apps/web. **apps/ssr type-check baseline = exactly 1 error**, `TS2307: Cannot find module '../../../public/unirefund.svg'` (src/components/global/logo.tsx) — same class of pre-existing image-module error as web's 2. apps/ssr has its own `init` and `type-check` scripts and must be gated separately in Task 6, which modifies it.
Full baseline set: apps/web type-check 2 errors; apps/ssr type-check 1 error; apps/web test:unit 134 tests / 133 pass / 1 skip / 0 fail; root lint exit 0 / 0 errors / 948 warnings.
Task 6: implementer DONE_WITH_CONCERNS — superproject `da18d2793`, submodule `2ba3a12`. Both apps mounted; all four gates at baseline (web tc 2, ssr tc 1, test:unit 134/133/1/0, lint exit 0 / 0 errors). Consumer diff empty for BOTH apps/web/src/app and apps/ssr/src/app.
Task 6: **Task 3 gap found late.** Task 3 created `app-config/fetch.ts` but never added a `"./app-config/fetch"` entry to packages/utils/package.json, so `@repo/utils/app-config/fetch` was unresolvable. Task 3's gates could not catch it because nothing imported it yet — Task 6 is its first consumer. Implementer added the export. Controller verified `app-config/index.tsx` still exports only ./logic and ./provider, so fetch.ts is reachable only by explicit server-side path and is NOT pulled into client bundles via the barrel.
Task 6: implementer also deleted apps/ssr's now-dead getApiRequests/ErrorComponent scaffold — a scope judgment call, passed to the reviewer unjudged.
Task 6: review package `review-0f8449c05..da18d2793.diff` (augmented to 266 lines, 4 files). Reviewer dispatched.
Task 6: review — Spec ✅ verified against source (both provider trees read in full, not the report's paraphrase), 0 Critical, 1 Important, 1 Minor. Reviewer independently validated the ssr scaffold deletion as CORRECT rather than merely defensible: it traced getGrantedPoliciesApi back through history and confirmed it has always caught internally and never thrown, so `Promise.all([...])` in ssr's old getApiRequests could never reject — that ErrorComponent path was dead code even before this task. It also confirmed nothing in getApplicationConfiguration's call graph calls Next's redirect(), so no in-flight redirect is being swallowed.
Task 6: **Ruling: UPHOLD the server-only finding.** Adding `"./app-config/fetch"` to packages/utils/package.json was necessary (both providers.tsx use the bare specifier, and the `"./*": "./*/index.tsx"` wildcard cannot resolve a file), but it turned a hard guarantee — fetch.ts categorically unresolvable outside the submodule — into a soft one. The task's own constraint says fetch.ts must NOT be client-reachable, and that now holds only incidentally, via node:path and ioredis three hops deep through auth/auth.ts. This monorepo already uses `import "server-only"` in four equivalent modules (apps/ssr get-translations, apps/web get-translations, apps/web document-extraction route, packages/ui document-capture handlers), so the guard is the established pattern and its absence is the defect. Fix: one line in fetch.ts. — Cost if wrong: none; `server-only` is a no-op on the server and only fails a client import, which is exactly the intent.
Task 6: **Ruling: scope the fix to fetch.ts only.** The reviewer suggested "ideally also auth/auth.ts and auth-actions.ts". Those predate this branch, auth-actions.ts already carries `"use server"`, and neither is a file this task made newly reachable. Adding guards there is unrelated hardening. — Cost if wrong: two other server modules keep the same latent softness they had before this branch; unchanged by us either way.
Task 6: minor (deferred): the submodule package.json edit was unplanned for a "mount the provider" task; visibility only, already documented in the implementer's report and here.
Task 6: CORRECTION to the reviewer's evidence — it claimed 4 files establish the `import "server-only"` pattern. Verified: only TWO are real (apps/ssr/src/language-data/get-translations.ts:1, apps/web/src/language-data/get-translations.ts:1); the other two were comments containing the words, not imports. The finding still stands on those two, but the precedent is narrower than stated.
Task 6: further verified before dispatching the fix — `server-only` is declared in NO package.json here, is absent from node_modules/.pnpm, and `require.resolve` fails from packages/utils, apps/web AND apps/ssr. Both get-translations.ts files nonetheless import it and both apps type-check clean, so Next resolves the specifier itself at build/type-check time. UNKNOWN: whether packages/utils' own standalone `tsc --noEmit` can resolve it, since that run is not Next-driven. Fix dispatched with an explicit stop-and-report instruction if that gate breaks, and a prohibition on working around it (no dep, no .d.ts shim, no paths mapping, no ts-ignore) — that trade-off is a controller ruling, not an implementer workaround.
Task 6: fix round 1/5 — guard ATTEMPTED and REVERTED by the implementer, correctly escalated instead of worked around. `packages/utils` tsc was clean with the guard; the blocker was a different gate: apps/web `test:unit` fell to 130/128 pass/1 fail because `resolve-tenant-names.test.ts` reaches fetch.ts transitively (resolve-tenant-names.ts → @repo/actions core/SaasService/actions.ts → @repo/utils/api → api/index.tsx re-exports api/action.ts → ../app-config/fetch) and `node --import tsx --test` cannot resolve `server-only` outside Next's bundler. No new commit; commits remain da18d2793 / 2ba3a12.
Task 6: **Ruling: DECLINE the server-only guard.** The finding was legitimate (hard guarantee became soft) but the remedy is disproportionate to it. The guard's only benefit is making a hypothetical future client import fail *loudly*; such an import already fails *today* because fetch.ts transitively pulls node:path and ioredis, neither of which bundles for a browser target — the protection exists, only its explicitness was missing. The available remedies are (a) reconfigure the test:unit harness with a server-only stub/loader, (b) restructure the @repo/utils/api barrel or make api/action.ts's import dynamic to break the static chain — a barrel ~130 call sites route through, or (c) decline. (a) and (b) both change working infrastructure to document a boundary that is already enforced by accident. Decision: decline, and spend one small round adding a comment to fetch.ts recording that it is server-only, why the `server-only` import is absent, and what enforces the boundary today — so the next engineer does not repeat this investigation or re-break test:unit. — Cost if wrong: if the auth/token-store chain ever becomes isomorphic, the incidental protection lapses and a client import would resolve. The comment names that exact condition as the trigger to revisit.
Task 6: fix round 2/5 — doc comment added to fetch.ts per the ruling; no guard, no dep, no shim. Commits submodule `f016811`, superproject `ae32c2a23`. All five gates back at baseline, with test:unit at 134/133/1/0 confirming the round-1 revert was complete. Controller verified only comment text mentions server-only (fetch.ts:17,23) — no import survived. Scoped re-review dispatched, asked to verify the comment's factual claims rather than just that the four points are present, since a wrong comment is worse than none.
Task 6: fix round 2/5 (2 addressed, 0 open; commits da18d2793..ae32c2a23) — re-reviewer verified the comment's two load-bearing factual claims by tracing the real chains: the import path (api/action.ts:2 → api/index.tsx:1 `export *` → packages/actions core/SaasService/actions.ts:11 → apps/web resolve-tenant-names.ts:1 → its test) matches the round-1 require-stack exactly, and the enforcement claim holds (auth/auth.ts:6 imports node:path directly; token-store.ts:1 imports ioredis). Also confirmed revert completeness repo-wide: no server-only import, dependency, .d.ts shim, tsconfig paths entry, or ts-ignore anywhere; the `"./app-config/fetch"` export correctly retained.
Task 6: complete (commits 0f8449c05..ae32c2a23, review clean after 2 fix rounds)
Task 7: **Ruling: add a production build of BOTH apps to Task 7's gates.** The plan's Task 7 checks only test:unit, type-check, lint and the consumer-diff assertion. None of those exercise Next's RSC client/server boundary, and this branch moved a server-only fetch behind a new package export and rewired two provider trees — exactly the class of change a build catches and tsc does not. It also directly tests the claim the server-only ruling rested on (that a client import of fetch.ts would fail to bundle). — Cost if wrong: a slower verification step. Not running it would leave the branch's riskiest property unverified.
Task 7: **Ruling: no task review for Task 7.** It changes no files, so there is no diff to review; its deliverable is evidence. The final whole-branch review covers the branch regardless. — Cost if wrong: none; a reviewer handed an empty diff has nothing to verdict.
Task 7: verification agent completed the read-only gates but stalled waiting on its own backgrounded `pnpm run build`, whose task died when the agent stopped (TaskStop reports no such task; its output file is 0 bytes). No concurrent-build risk. Controller is running the build itself and will hand the result back to the agent so the report stays a single artifact.
Task 7: BUILD GATE — first run FAILED, root-caused to the environment, NOT the branch. `apps/ssr` failed to compile at src/app/[lang]/layout.tsx with "An error occurred in `next/font` / Error: Cannot find native binding", thrown from node_modules/.pnpm/@tailwindcss+oxide@4.1.16/.../index.js:562 — Tailwind v4's native engine. Evidence it is environmental: (1) the branch diff touches only the two providers.tsx, tenant.tsx, is-host-tenant.ts, four test/fixture files and the packages/utils pointer — no Tailwind, CSS, font or layout file; (2) `apps/ssr/src/app/[lang]/layout.tsx`, the sole next/font consumer, is not in the diff; (3) the win32 binding package IS in the store with its .node file present, but alongside it sits a leftover `@tailwindcss/oxide-win32-x64-msvc_tmp_37800_451/` directory — a pnpm interrupted-install artifact, which is the fingerprint of the ERR_PNPM_EPERM failure this worktree's first `pnpm install` hit on next-swc. apps/web did not fail; it was still compiling when turbo aborted on ssr. Repairing with `pnpm install --force` and re-running the build.
Task 7: verification agent reported DONE_WITH_CONCERNS. PASS: clean tree (both repos); unit tests 134/133/1/0 exactly at baseline; web type-check exactly 2 unirefund.png errors; ssr type-check exactly 1 unirefund.svg error; lint 0 errors / 948 warnings; `cache()` wrapping and getGrantedPoliciesApi rewiring quoted and verified; provider mounted in BOTH apps wrapping all context consumers. **Consumer diff for apps/web/src/app AND apps/ssr/src/app is EMPTY — the non-breaking migration across ~267 consuming files is proven, not asserted.**
Task 7: the agent independently root-caused the build failure and sharpened my diagnosis: `@tailwindcss/oxide-win32-x64-msvc@4.1.16`'s pnpm store entry is missing its `package.json` (only the compiled .node is there), so Node cannot resolve the package at all. It reproduced this with a bare `require()` outside any bundler, and confirmed the branch changes no package.json or pnpm-lock.yaml. Same failure in both Turbopack (web) and webpack (ssr). Two independent diagnoses agree: environment, not branch.
Task 7: backend ticket DRAFTED in task-7-report.md and explicitly NOT filed — creating an issue in the user's tracker is outward-facing and reserved for them.
Task 7: build repair attempt 2 — `pnpm install --force` SEGFAULTED (exit 139) and left the build failing identically. Inspected the store entry directly: node_modules/.pnpm/@tailwindcss+oxide-win32-x64-msvc@4.1.16/.../oxide-win32-x64-msvc/ contained ONLY tailwindcss-oxide.win32-x64-msvc.node — no package.json, no LICENSE, no README — confirming a corrupt/partial extraction from the EPERM-interrupted first install. The user's main checkout has an intact copy of the identical version; copied the three missing files in (read-only on their checkout, writing only into the worktree's gitignored node_modules) and re-ran the build. This is environment repair, not a code change — nothing tracked by git was touched.
Task 7: **BUILD GATE NOW PASSES.** After the surgical store repair: `web:build: ✓ Compiled successfully in 86s`, `ssr:build: ✓ Compiled successfully in 2.1min`, `Tasks: 2 successful, 2 total`, with ssr generating 8 static pages. This definitively confirms the failure was the corrupt @tailwindcss/oxide store entry and NOT the branch, and it validates the reasoning behind the server-only ruling: a production build of both apps finds no RSC client/server boundary violation.
Task 7: complete — all gates green. Clean tree; unit 134/133/1/0; web type-check 2 baseline; ssr type-check 1 baseline; lint 0 errors/948 warnings; consumer diff EMPTY for both apps; cache() + getGrantedPoliciesApi rewiring verified by quote; provider mounted in both apps; production build of both apps PASSES. No task review (no diff — ruled above).
FINAL REVIEW (opus, whole branch 870b40710..ae32c2a23): **Ready to merge WITH FIXES.** 0 Critical, 6 Important, 10 Minor. All 13 controller rulings independently reviewed and AGREED with, several strengthened.
FINAL REVIEW — **my "~128 calls per page render" figure is WRONG by ~10x.** I conflated repo-wide isUnauthorized call SITES (135, of which 127 fall through) with calls EXECUTED per render. Next renders one page plus its layout chain; the reviewer measured the distribution (126 files have 1 call, tax-free-tags/[tagId] has 9-10, and the [lang] and (main) layouts contribute 0). True numbers: typical route 2 -> 1, worst route 11 -> 1. Still real (~225ms x 10 on the tag detail page) but not what the plan, spec and fetch.ts docblock claim. Root cause of the error surviving: plan Task 6 Step 4 asked for an end-to-end request count and that step was NEVER RUN.
FINAL REVIEW — must-fix before merge: (1) getNumberSetting returns 0 for "" because Number("")===0, and the spec names six numeric password-policy keys as next consumers — a silent RequiredLength:0 is security-relevant, and plan 4 copies this file verbatim into pos-app; (2) correct the request-count figure in fetch.ts, the plan AND the spec; (3) the headline claim has never been observed in a browser once.
FINAL REVIEW — delivery blocker: the submodule commits 0b1ddaa..f016811 are NOT PUSHED (`git branch -r --contains f016811` is empty), so the superproject's pointer references a commit no other clone or CI runner can resolve. Expected for two-PR delivery but must be stated.
FINAL REVIEW — reviewer strengthened the server-only ruling: the DURABLE enforcement is structural, not incidental — both client-reachable routes (api/action.ts, policies/utils.ts) are `"use server"` modules whose bodies Next never bundles, and the app-config barrel exports only pure modules. It also closed a fourth remedy I had not considered: adding `server-only` as a devDependency would ALSO fail test:unit, because its exports map resolves to a throwing index.js outside the react-server condition. Ruling is robust.
FINAL REVIEW — pre-existing security issue found, OUT OF SCOPE and untouched by this branch: packages/utils/auth/auth.ts:228-243's session callback re-hydrates BOTH access_token and refresh_token onto session.user, and session.tsx re-exports next-auth's SessionProvider unmodified — so bearer credentials already reach the browser on every authenticated page, one line below the new provider. Needs its own ticket; far larger than this branch's PII delta.
BASELINE CORRECTION (third and final on this figure): the apps/web "exactly 2 TS2307" and apps/ssr "exactly 1 TS2307" baselines were artifacts of a never-built worktree, NOT a repo property. `next-env.d.ts` is gitignored (.gitignore:159) and generated by `next build`/`next dev`; it carries `/// <reference types="next/image-types/global" />`, the declaration that teaches tsc about *.png / *.svg imports. The image files themselves (apps/web/public/unirefund.png, apps/ssr/public/unirefund.svg) are TRACKED and were present throughout — the tree never showed them as deleted. Once the Task 7 build generated next-env.d.ts, all three errors vanished. **True baseline for both apps is 0 type errors, and that is where the branch now sits.** All the per-task "exactly 2 / exactly 1" comparisons were still valid like-for-like at the time they were made; only the stated cause was incomplete. (First I wrote it off to `mrz`/GitHub Packages, then corrected to unirefund.png, now root-caused properly.)
FINAL FIX WAVE: DONE, all 9 fixes applied. Submodule `1f2dce8`, superproject `34e76e8fa`. Gates: packages/utils type-check clean; apps/web type-check 0 errors; apps/ssr type-check 0 errors; test:unit 136/135 pass/1 skip/0 fail (+2 empty/whitespace numeric cases); root lint exit 0 / 0 errors / 948 warnings exactly; root build Tasks 2 successful / 2 total; consumer diff for both apps EMPTY. Implementer extended Fix 7 slightly — also dropped the now-dead `lang` destructure in apps/ssr providers, since leaving it bound-but-unused would have added a lint warning above the expected count; verified with direct eslint runs.
FIX-WAVE RE-REVIEW: all nine fixes ADDRESSED, no new Critical/Important breakage, **Ready to merge: Yes**. Fix 8 (parallelizing the provider awaits) — the flagged highest-risk item — checked out clean: no data dependency between the two calls, auth() still ordered first, the `"message" in apiRequests` branch and optionalRequests destructuring untouched, and getApplicationConfiguration never rejects so Promise.all is equivalent on error paths. Fix 1's new tests were hand-traced against the pre-fix body and would genuinely have failed (Number("")→0, non-NaN, returned 0 vs expected 8).
FINAL RULING (residual Minor, adjudicated — no second fix wave per process): **stop asserting a per-render request count anywhere.** The re-reviewer traced the "worst route" claim and found that of the 9 isUnauthorized sites in operations/tax-free-tags/[tagId]/page.tsx, 8 explicitly pass an already-resolved grantedPolicies from one prior call (line 135), which short-circuits `initalGrantedPolicies ?? (await getApplicationConfiguration()).policies` — so its static count is ~3, not 11. That means the "11" I substituted for the wrong "~128" is ALSO unverified: two successive static estimates, both wrong, because call-site counting cannot predict request counts when call sites hand each other resolved values. Decision: correct the spec and plan (my own artifacts) to state plainly that the per-render count is UNMEASURED and that the cache() justification does not depend on it; park the single "9 call sites of its own" clause at packages/utils/app-config/fetch.ts:49 for the human to trim, since process forbids a second fix wave and it is a documentation-only inaccuracy in one comment. — Cost if wrong: a code comment overstates a benefit that is real but unquantified. The dedup itself is proven by construction (cache() memoizes per request) and by the empty consumer diff; only its magnitude is unknown.
