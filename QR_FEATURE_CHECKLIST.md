# Traveller QR Feature — Implementation Checklist

Resumable tracker for the traveller QR-scanning feature (claim draft tag / view issued tag / airport self-validation). Continue from the first unchecked box. Full design lives in the approved plan; this is the working state.

**Decisions:** all three flows · scan-before-login (web-style, public read then login/KYC to claim) · full airport self-validation. Reference: `unirefund-web/apps/ssr` (`/tag/[slug]`, `/validate?qrValue=`).

**Status note:** `ExportValidationService` SDK is generated (`client.qrEvidence`), in `API_LIST.json`, and `getExportValidationServiceClient` exists in `src/actions/lib.ts` — so the airport flow is NOT blocked.

---

## Phase 0 — Dependencies & config
- [x] `expo-camera` (~17.0.10) + `expo-location` (~19.0.8) added to `package.json`.
- [x] `expo-camera` + `expo-location` config plugins in `app.config.js` (permission strings).
- [x] `swagger-json/ExportValidation` in `src/saas/API_LIST.json`; service generated. *(done by user)*
- [x] `getExportValidationServiceClient` in `src/actions/lib.ts`. *(done by user)*

## Phase 1 — Shared QR infra
- [x] `src/utils/qr/base64url.ts` — RN-safe base64url decode.
- [x] `src/utils/qr/tagSlug.ts` — `slugFromScan` + `decodeTagSlug` → `{tagNumber,tagId,travellerDocumentNumber,stickerLineNumber}`.
- [x] `src/utils/qr/classifyScan.ts` — `classifyScan(raw)` → tag | validate | unknown.
- [x] `src/components/QrScanner.tsx` — CameraView `onBarcodeScanned` modal + permission gate + overlay.

## Phase 2 — Tag flows (claim draft / view issued), pre-login capable
- [x] Tag action wrappers (`src/actions/TagService/actions.ts`): `getPublicTagByTagId`, `getPublicTag`, `postTagTravellerSelfAssign`, `postTagTravellerSelfAssignByTagId`, `getOwnedTagByTagNumber`; `salesAmountFromPublicDetail`/`isDraftPublicTag` in `src/utils/tag.ts`.
- [x] `src/store/pendingScan.ts` — deferred-intent store (survives (public)→(auth) swap).
- [x] `src/screens/TagPreviewScreen.tsx` + **root** route `src/app/tag-preview.tsx` (root, not (public), so it works logged-in or out). Draft→Claim / issued→view; "Login to claim" stores intent → `/traveller-login`.
- [x] Claim completion → `postTagTravellerSelfAssign` → toast → `/(auth)/tags/[id]`.
- [x] Deferred-intent resume after login — `src/hooks/useResumePendingScan.tsx`, called in `src/app/(auth)/_layout.tsx`.
- [x] Scan entry points: pre-login button on `TravellerLoginScreen`; traveller CTA on Home. Shared `src/hooks/useQrScanLauncher.tsx` (classify → route).
- Note: `/tag-preview` + `/validate` registered as **root** routes in `src/app/_layout.tsx`; typed-routes regenerated.

## Phase 3 — Airport self-validation (full)
- [x] `src/utils/location.ts` — expo-location permission + current position.
- [x] `src/utils/qr/bcbp.ts` — IATA BCBP Type-M parser → FlightTicketInfoDto (+ `hasParsedFlightInfo`).
- [x] Validate screen `src/screens/ValidateScreen.tsx` (root route `src/app/validate.tsx`) — auth gate (defer→login) → geolocation → flight ticket (scan/manual) → scan → results, with qr-inactive rescan + session-expired + failed states.
- [x] `src/screens/Validate/FlightInfoStep.tsx` (scan boarding pass / manual) + `src/screens/Validate/ScanResultView.tsx` (green/already/red+exit points/rejected); enrich via `getTags({tagIds})`.
- [x] `src/actions/ExportValidationService/actions.ts` `postQrEvidenceScan(...)` → `client.qrEvidence.postApiExportValidationServiceQrEvidenceByQrValueScan`. *(SDK generated by user — unblocked.)*
- [x] Validate state machine wired to real scan + `ScanResultDto`.
- [ ] FOLLOW-UP (not done): the web also lets you claim more tags from the results view (claim modal + "latest claimed" highlight) and re-scans on modal close. Mobile does the core scan→results + rescan-on-expiry; in-results claiming is deferred.

## Phase 4 — Localization, wiring, verification
- [x] Registered `tag-preview` + `validate` as **root** routes in `src/app/_layout.tsx`; typed-routes regenerated.
- [x] i18n keys added to the **source** `src/localization/resources/{en-US,tr-TR}.json` (NOT the generated files — `init.ts` regenerates `*.gen.json` by merging server localization + these resources, so source is the only place edits survive). Run `npm run init` to regenerate. Keys: `RoleGate.*`, `Auth.Traveller.*`, `Auth.Staff.*`, `Qr.*` (incl. `TagPreview.*`, `Validate.*`).
- [x] `npx tsc --noEmit` clean (EXIT 0).
- [ ] Native rebuild required (expo-camera/expo-location are native): `npm run android` / `npm run ios` (or `expo prebuild`), then manual verification of all three flows (see plan Verification).

## Phase 5 — Sticker QR flows (2026-07-29)
Design: `docs/superpowers/specs/2026-07-29-sticker-qr-scan-flows-design.md`
Plan: `docs/superpowers/plans/2026-07-29-sticker-qr-scan-flows.md`

- [x] `classifyScan` returns `kind: "sticker"` for a slug carrying only `s`. A slug with tag fields *and* `s` stays `tag`; a bare number stays `unknown` (mobile has no wedge scanner, unlike the web page).
- [x] Sticker actions in `src/actions/TagService/`: `getStickerLineByNumber`, `getStickerLineMerchantInfo`, `getMerchantsForTagCreation`, `getPublicTagByStickerLineNumber`, `postTagByStickerLine`. No SDK regeneration was needed.
- [x] Traveller / logged-out: sticker → `/tag-preview`, resolved anonymously by sticker line number, with a dedicated "no tag on this sticker yet" state.
- [x] Calculator moved to `src/screens/shared/_components/tag-calculator/`; `SignaturePads` gained `targets`; `useMerchantId` moved to `src/hooks/`.
- [x] Staff: sticker → `/sticker-tag`. Merchant resolved from the sticker line (allocated read-only / merchant-is-self / refund-point picker), required invoice number, optional traveller, traveller signature, `POST /tag/by-sticker-line`.
- [x] A sticker that already carries a tag opens that tag instead of a create form.
- [x] Merchant staff resolve the sticker's merchant **without** `TagService.StickerHeaders.ViewMerchantInfo`, which that role does not hold: allocation comes from `stickerLine.merchantId`, identity from the sticker line or CRM, product groups from `getProductGroupByIdApi` — the same path `CreateTagScreen` already uses. Only Refund Points call the merchant-info endpoint. A merchant scanning a book allocated to a *different* store gets a terminal "belongs to another store" state.
- [x] Shared cart maths (`lineFromAmount`, `mergeLine`) extracted to `tag-calculator/line.ts` and used by both the merchant and sticker flows, so the two carts cannot drift.
- [ ] Native verification on device: scan a printed sticker as a merchant, as a refund point (allocated and unallocated books), and as a traveller.

## Phase 6 — Manual entry and 1D barcodes (2026-07-29)
Design: `docs/superpowers/specs/2026-07-29-manual-code-entry-design.md`
Plan: `docs/superpowers/plans/2026-07-29-manual-code-entry.md`
Catalogue: #9, #18, #28

- [x] `classifyScan(raw, source)` takes the barcode symbology. A bare value from a 1D read is a tag number; from a QR read it stays `unknown`, so a half-decoded QR still cannot be looked up as an identifier.
- [x] `scanDestination(classification, isStaff)` extracted from `useQrScanLauncher` — pure, tested, and shared with manual entry. A bare tag number, which previously dead-ended at "not found", now resolves per role.
- [x] `useScanRouting` owns the effects: navigate, toast, or resolve a tag number to its id via `getTagDetailByTagNumber`.
- [x] `/manual-entry` root route with Sticker and Tag modes. Staff see no passport field; a traveller's public read needs one. Every mode exits through the same `routeScan` the scanner uses.
- [x] "Can't scan? Enter it manually" on the scanner overlay, wired at the four call sites that scan Unirefund codes.
- [x] Two scanners deliberately have no manual-entry link: the boarding-pass scanner in `Validate/FlightInfoStep.tsx` (an IATA barcode, and it already has its own manual tab) and the rolling-QR rescan in `ValidateScreen.tsx` (server-issued, nothing human-readable to type).
- [ ] Native verification on device: scan a POS Code128, type a sticker number as each role, and type a tag number as staff and as a traveller.

## Phase 7 — Role-correct tag creation (2026-07-30)
Design: `docs/superpowers/specs/2026-07-30-role-correct-tag-creation-design.md`
Plan: `docs/superpowers/plans/2026-07-30-role-correct-tag-creation.md`
Catalogue: #29, and #15 on web

- [x] Merchants create through `POST /tag` (`postTagServiceTagApi`), which their `Tags.Create` grant covers; Refund Points keep `POST /tag/by-sticker-line`, which needs `Tags.CreateByStickerLine`.
- [x] The sticker still binds in one call — `CreateTagRequestDto.stickerLineNumber` — rather than needing a separate assign.
- [x] `buildMerchantTagRequest` added beside `buildStickerTagRequest`, both pure and tested, with a test asserting neither body carries the other's discriminating field.
- [x] `useStickerLine` returns `merchantIdentity`; `externalIdentifier` was previously fetched from CRM and silently dropped, because the generated DTO it mapped into has no such field.
- [x] Merchants capture both signatures on mobile; Refund Points keep the traveller pad only, their DTO having nowhere to put a merchant signature.
- [x] Web merchants no longer call `getStickerLineMerchantInfoApi` (#15/#26) — identity from the sticker line or CRM, product groups from CRM. Refund Points keep it.
- [ ] Native and browser verification: create a tag from a sticker as a merchant and as a refund point, on an allocated and an unallocated book, in both apps.
